# Mochi 免费搜索能力调研

日期：2026-09-06 · 范围：GitHub 开源 / 免费额度检索能力 · 目标：范围广、来源精度高、准度高

---

## 0. 结论先行

**不要找"一个"搜索工具，要搭三层。** 三个诉求互相拉扯，只能靠分层解决：

| 诉求 | 由哪层承担 | 具体选型 |
|---|---|---|
| 范围广 | 元搜索层 | **自建 SearXNG**（Bing/Baidu/Google/Startpage/DDG 多引擎扇出，200+ 引擎、30+ 分类） |
| 来源精度高 | 权威源层 | **OpenAlex + Crossref + arXiv + Europe PMC + 中文维基/Wikidata + 教材权威域名**——全部无 key、可商用级 CC0/CC-BY |
| 准度高 | 精排 + 正文抽取层 | **Jina Reader 抽取正文** + 轻量 rerank（Jina Reranker 免费额度，或本地 bge） |

**对现有插件最关键的一条改造**：`mochi-web-search` 现在把 `trustedDomains` 当**过滤器**用，5 个域名直接把绝大多数查询过滤到 0 结果。必须改成**加权排序**——白名单域名加分、但不丢弃其他结果。这是同时拿到"范围广"和"来源精度高"的唯一办法，靠换工具解决不了。

---

## 1. 现状诊断：现有 `mochi-web-search` 的三个真实缺口

文件：`plugins/mochi-web-search/index.mjs`（137 行，已注册到官方 `ctx.web`，provider id = `mochi-free-web`）

1. **范围极窄**：`TEACHER_AUTHORITY_DOMAINS` 只有 5 个域名（basic.smartedu.cn / fltrp.com / moe.gov.cn / openstax.org / pep.com.cn）。SearXNG 返回后逐条做主机名校验，不在白名单的直接丢弃 → 大多数查询**返回 0 条**，然后静默降级到中文维基。
2. **实际一直在跑兜底**：`MOCHI_SEARXNG_ENDPOINT` 未配置（README 明确"不随机依赖公共实例"），所以 `this.searxngEndpoint` 为 undefined → 当前生产行为 = 100% 中文维基百科。
3. **没有正文抽取和重排**：只返回 url/title/snippet。snippet 是搜索引擎给的摘要，不是原文——"准度高"没有落点，模型只能靠 snippet 猜。

结论：**工具选型的差距远小于这三条架构问题的差距**。换任何后端，只要保留"硬白名单过滤 + 无正文"，效果都不会变好。

---

## 2. 候选全景（GitHub / 免费额度）

### A 类 · 完全免费、无需 API Key（自托管或公共端点）

| 项目 | 类型 | 覆盖 | 代价 / 风险 | 适配度 |
|---|---|---|---|---|
| [searxng/searxng](https://github.com/searxng/searxng) | 元搜索引擎 | 70–200+ 引擎、30+ 分类（news/science/it…） | 需自建；**JSON 输出要实例显式开启**，多数公共实例禁用且会 429 | ★★★★★ 主力 |
| [ihor-sokoliuk/mcp-searxng](https://github.com/ihor-sokoliuk/mcp-searxng) | SearXNG MCP（TS, MIT） | 多实例 failover、`min_score` 相关性过滤、分页/时间/语言、TTL 缓存、URL 正文抽取 | 需自建 SearXNG；1.1k stars，活跃维护 | ★★★★☆ |
| [whw23/searxng_http_mcp](https://github.com/whw23/searxng_http_mcp) | SearXNG MCP（Py, 内置 SearXNG） | **Docker 内置 SearXNG**，零安装；多页 fanout、自动补全、引擎发现 | 一条 `docker run` 起全套，最省事 | ★★★★★ 最快落地 |
| [TadMSTR/searxng-mcp](https://github.com/TadMSTR/searxng-mcp) | SearXNG + 本地 ML 重排 | 4 级抓取级联（Firecrawl/Crawl4AI/raw/Wayback）+ 按域名学习 + Ollama 查询扩展 | 组件重（要 Redis、Firecrawl、Ollama） | ★★☆☆☆ 过重 |
| [lightdot/open-webSearch](https://github.com/lightdot/open-webSearch) | 直解 HTML（Node） | **bing / baidu / duckduckgo / exa / brave / csdn / juejin**，无 key | 刮 HTML，结构一变就挂；有 ToS 与封 IP 风险 | ★★★☆☆ 中文覆盖好但脆 |
| [vitkuz573/mcp-web-search](https://github.com/vitkuz573/mcp-web-search) | 直解 HTML（Rust） | Google/Bing/Brave/DDG/Yahoo/YouTube，TOML 自定义引擎 | 同上，刮解析 | ★★★☆☆ |
| [moreti2002/polymath-mcp](https://github.com/moreti2002/polymath-mcp) | **学术聚合 MCP（MIT）** | arXiv / Crossref / DBLP / Europe PMC / OpenAlex / CORE / Zenodo / Semantic Scholar / Unpaywall / OpenCitations / GitHub / HF | 无 key 可用；已内置每源节流 + 去重 + 失败隔离 | ★★★★★ 精度层首选 |

### B 类 · 免费额度 API（需注册，多数不要信用卡）

| 服务 | 免费额度 | 特点 | 备注 |
|---|---|---|---|
| **Jina** | 10M tokens（注册即得，Reader/Embeddings/Reranker 共享）；`r.jina.ai` 无 key 也能用（限速） | 最便宜的 URL→markdown 正文抽取；也有 Search 端点 | **性价比最高，优先拿** |
| **Serper** | 2,500 次（一次性注册赠） | Google SERP JSON，$0.30–1/1k，最便宜的 Google 结果 | 一次性，用完后按量 |
| **Tavily** | 1,000 credits/月（刷新） | 为 Agent 设计，返回已清洗内容 + 可选带引用的 answer；官方 MCP | 2025 被 Nebius 收购 |
| **Exa** | 1,000 次/月（另有资料称 $10 额度，**口径冲突，需自查**） | 语义/神经检索，`find similar`；官方 MCP | 小众查询覆盖不如关键词 |
| **Brave Search** | 2,000 次/月（一份资料称 **2026 已取消纯免费档、需绑卡 $5/月额度**，**口径冲突，需自查**） | **独立索引**，非 Google/Bing，来源多样性价值高 | 冲突未决前不押注 |
| **Firecrawl** | 500 页/月 | 抓取 + 抽取一体 | 额度偏紧 |
| SerpAPI | 250 次/月 | 多引擎 SERP，最贵 | 额度太小 |

### C 类 · 高精度垂类源（全部无 key，"来源精度高"的真正来源）

| 源 | 覆盖 | 无 key 限额 | 备注 |
|---|---|---|---|
| **OpenAlex** | 2.4–2.5 亿作品、25 亿引用 | 加 `mailto` 进 polite pool 基本无限；否则 10 req/s | **CC0，覆盖约为付费库的 2 倍**，通用学术首选 |
| **Crossref** | 1.8 亿 DOI | 加 `mailto` 进 polite pool | 权威出版元数据，无摘要 |
| **arXiv** | 200 万+ 预印本 | 官方要求 1 req/3s | 返回 Atom XML，需解析 |
| **PubMed E-utilities** | 3,660 万条 | 3 req/s（有 key 10） | 生物医学 |
| **Europe PMC** | 4,000 万+ | ~10 req/s | 可拿全文 |
| Semantic Scholar | 2 亿+ | 共享匿名池，**频繁 429** | 只能 best-effort，不可阻塞 |
| Unpaywall | OA 全文 PDF 解析 | 需 email 参数 | 配合 DOI 用 |
| DBLP / bioRxiv·medRxiv / OpenCitations / Zenodo | 各异 | 宽松 | 补充 |
| Wikipedia / Wikidata | 全语种百科 + 结构化事实 | 宽松 | 现有插件已在用维基，可补 Wikidata 做实体事实 |

> 注：Papers with Code 已于 2025 停服，其位置由 HuggingFace Papers 顶上。
> 中文权威源（国家智慧教育平台、人教社、外研社、教育部）**没有公开检索 API**，只能靠"域名加权 + 通用搜索引擎 site 内检索结果"间接拿到——这正是现有白名单的正确用法。

---

## 3. 推荐架构

```
                    ┌────────────────────────────┐
   模型 web_search ─▶│  融合层（要新写，核心改造点）  │
                    │  · 扇出 → 去重 → 加权 → 重排  │
                    └───┬──────────┬──────────┬───┘
                        │          │          │
         ┌──────────────▼┐  ┌──────▼──────┐  ┌▼────────────────┐
         │  L1 范围层     │  │ L2 权威层   │  │ L3 精排/正文层   │
         │ 自建 SearXNG   │  │ OpenAlex    │  │ Jina Reader     │
         │ Bing/Baidu/    │  │ Crossref    │  │ (url→markdown)  │
         │ Google/DDG/    │  │ arXiv       │  │ + Reranker      │
         │ Startpage      │  │ Europe PMC  │  │ (Jina / bge 本地)│
         │ 200+ 引擎      │  │ Wikidata    │  │ + 权威域名加权   │
         └────────────────┘  │ 教材域名白名单│  └─────────────────┘
                             └──────────────┘
```

**融合层是准度的全部来源**，四步：

1. **扇出**：L1 + L2 并发查，任一失败不阻塞（现有插件的 SearXNG→维基是"串行降级"，L1 一挂就退化）。
2. **去重**：按 URL 规范化（去 utm 等追踪参数）+ 标题相似度。
3. **加权**：权威域名 / .edu / .gov / 教材社 / 被 Crossref 收录 → 加分；内容农场、SEO 站 → 减分。**加权不是过滤**。
4. **重排 + 抽取**：Top-K 走 Jina Reader 拿正文，再用 reranker 对"查询-正文"打分，只把 Top-N 正文喂给模型。

---

## 4. 落到 Harness 的两条路径

内核已具备（`mochi-harness-src.nosync/mochi-harness/packages/web/`）：

- `ctx.web` 服务 + `web-search-exa` / `web-search-perplexity` / `web-search-deepseek` 三个官方后端
- `web-fetch-http`（匿名 HTTP 抓取）、`tool-web`（向模型暴露 `web_search` / `web_fetch`）
- `packages/mcp/mcp-client` 可挂任意外部 MCP server（**只桥 Tools，不桥 resources/prompts**）
- `tool-web` 配置项：`searchMaxResults`(8)、`searchMaxQueries`(4)、`fetchTimeoutMs`(30000)

**路径 1（推荐）：继续写 `ctx.web` provider**
- 与现有 `mochi-web-search` 同构，`DSH_WEB_SEARCH_PROVIDER=mochi-free-web` 已能锁定
- 完全掌控融合层、白名单、隐私门禁——Mochi 有学生数据红线，这层必须自己握着
- 缺点：融合层要自己写

**路径 2（快）：`mcp-client` 挂现成 MCP server**
- 最快拿到"范围广"：`whw23/searxng_http_mcp` 一条 docker run 自带 SearXNG
- 最快拿到"精度高"：`moreti2002/polymath-mcp` 直接给一堆学术源
- 缺点：工具面由对方定义，融合/加权/隐私门禁插不进去；多 server 反而让模型自己挑，准度不稳

**建议混用**：L1/L2 用 `ctx.web` provider 自己写，**L3 的正文抽取直接用现成的 Jina**（不用自研抽取器）。

---

## 5. 针对现有插件的 5 个具体改造点

| # | 改什么 | 现状 | 改成 |
|---|---|---|---|
| 1 | `trustedDomains` 语义 | 硬过滤，5 域名外全丢 | **打分权重**，`isTrustedHostname` 返回值改成权重而非 boolean；未命中也能进结果集 |
| 2 | 兜底策略 | SearXNG 挂了才降级维基（串行） | L1/L2 **并发扇出 + 合并**，任一源失败只记录不影响返回 |
| 3 | 正文抽取 | 无 | 对 Top-K 调 Jina Reader（无 key 先用 `r.jina.ai`，顶不住再上 key 拿 10M token） |
| 4 | 重排 | 无（靠 SearXNG 原序） | 查询-正文交叉编码打分（Jina Reranker 免费额度；要完全离线就本地 bge-reranker） |
| 5 | 治理 | 只有 8s 超时 | 加：每源并发上限、日额度计数（保住免费档，防止 Agent 重试把月额度刷爆）、按域名缓存 TTL、失败熔断 |

---

## 6. 风险与红线

- **学生数据**（继承红线）：`ctx.web.search` 的请求体只有一个裸 query 字符串，provider 无法识别其中是否夹带学生姓名/学号/健康/成绩。现有 README 已声明"由调用方 skill 先过隐私门禁"——**新增任意外网检索源后这条仍然成立，且更容易被绕过**，接入后必须复查 `skills/teaching-material-find/` 的门禁是否被执行。
- **刮解析类项目的 ToS 风险**：`open-webSearch`、`mcp-web-search` 这类直解搜索引擎 HTML 的项目，法律上处灰色地带、技术上引擎改版即挂。**可以做兜底，不要做主力**。
- **Brave / Exa 免费档口径冲突**：两份 2026 资料的免费额度说法不一致，落地前必须自己注册验证，不要照抄本报告数字。
- **版权**：教材与配套资源正文版权须逐页确认。现有插件只返回链接和摘要的做法是对的，**加了 Jina 正文抽取后这条边界会被突破**——抽取正文前要明确"引用片段"与"复制全文"的界限。
- **合规**：自建 SearXNG 要遵守各引擎 robots；对外服务时公共实例 429 是常态，别依赖。

---

## 7. 分场景选型建议

| 场景 | 首选 | 备选 |
|---|---|---|
| 教师找备课资料（中文为主） | SearXNG(baidu+bing) + 教材域名加权 + Jina 正文 | open-webSearch 兜中文社区 |
| 学术/教研文献 | OpenAlex → Crossref → arXiv → Europe PMC | polymath-mcp 一步到位 |
| 事实核查 / 百科 | Wikidata + 中文维基 | — |
| 英文通用 | SearXNG(google/brave/startpage) | Tavily 免费档 |
| 只想最快看到效果 | `whw23/searxng_http_mcp` 一条 docker | — |

---

## 8. 建议的下一步（按投入排序）

1. **先测**：本地起 `whw23/searxng_http_mcp`（自带 SearXNG），把 `MOCHI_SEARXNG_ENDPOINT` 指过去，验证现有插件的真实效果上限。成本 ≈ 一条命令。
2. **再改**：把 `isTrustedHostname` 从过滤改加权（约 20 行改动），这是投入产出比最高的一处。
3. **后扩**：加 OpenAlex + Crossref 两个无 key 源做 L2（约 100 行），拿到"来源精度高"。
4. **最后**：接 Jina Reader 做正文 + rerank，拿到"准度高"。
