# 2026-09-16

## 只读审核：项目当前的不完善之处（用户并行开发中，全程未跑测试、未改源码）

**背景**：用户 23:12 提问「就项目本身而言还有什么不完善的地方」。当时 `test-installer-config.mjs` 1 分钟前刚被改（23:11），确认用户正在活跃开发 → 采取纯只读勘察。

### 🔴 最硬发现：快照清单全面未同步（提交即挂 CI）

`.github/windows-native-package-inputs.json` 在 `git status` 中**零改动**，但本轮 60 个改动里有 **30 个落在快照范围**：

1. **27 个已在清单内的文件内容已变**，哈希未重算。`check-snapshot-manifest.mjs` 实测：
   - 清单声明总字节 **91,963,933**，磁盘实际 **91,943,606**，字节差 **−20,327**
   - 不一致 **27 个**，缺失 0 个
   - ⇒ CI 首步 `node scripts/check-snapshot-manifest.mjs --fail` 退出码 1，**Windows CI 白跑一轮**
2. **3 个新文件该进清单但没进**（白名单已登记、清单漏登记）：
   - `plugins/mochi-hello/work-quality.mjs`
   - `plugins/mochi-hello/work-quality.md`
   - `plugins/mochi-presentations/process-layout.mjs`
   - `prepare-mochi-resources.cjs` 的 `PLUGINS` 白名单**已加**这 3 个（git diff 可见），但清单里 mochi-hello 只有 `doctor.mjs/index.mjs/package.json` 三条
   - ⇒ 即使修好哈希，Windows 包仍会**缺这 3 个文件** → 运行时模块找不到
   - `expectedFileCount` 496 → 应为 499；`expectedFileBytes` 需重算

**修复**：`node scripts/reconcile-snapshot-manifest.mjs --write`（只读模式不加 `--write` 即只报告）

⚠️ `docs/agent-quality.md:29` 写「打包files名单包含两个新资源，npm pack dry-run已核实」——**该核实只覆盖 npm pack，不覆盖 Windows 快照清单**，不能据此认为出包无风险。

### 🟡 CI 覆盖缺口：`npm run check` 的 4 个新入口 CI 完全不跑

根 `package.json:17` 的 `check` 含 `test:prompt-quality` / `test:eval-evidence` / `test:model-transport` / `test:compaction`；
但 `.github/workflows/mochi-ci.yml` 的 quality job 是**逐条分步**（只到 `test:core-plugins`），**不跑 `npm run check`**，也不含这 4 个入口。
⇒ 文档宣传的「144/151 项全通过」是**本地结论**，CI 从不验证。这是「规范承诺与执行脱节」的第 4 次。

### 🟡 提交卫生：`evals/` 27 MB 未被 gitignore

- `evals/` 共 120 文件、**27 MB**，全部未跟踪（`?? evals/`）
- 含 3 个 **6.3 MB** `presentation.pdf`（同一 water-cycle 产物的三个版本）+ `results.json` 1.9 MB + `rescore-results.json` 1.5 MB
- `.gitignore` 已排除 `artifacts/`（理由：体积大、过程性），**但没排除 `evals/`** → `git add -A` 会把这 27 MB 全入库，与项目自身约定不一致

### 🟡 明文密钥仍在（09-14 发现，至今未处理）

`credentials-seed.json`（209 B，含 `sk-` 明文）仍在 4 处：
`resources/mochi-web/seeds/`、`.mochi-package-resources-v1.nosync/profile/seeds/`、`release/mac/Mochi.app/…`、`release/mac-arm64/Mochi.app/…`

### 🟢 其他

- `docs/README.md` 未登记 `DECISIONS.md`（其余 QUALITY-GATES / DOCUMENT-INVENTORY 及 4 份新文档**均已登记**，此前记录已过时）
- `mochi-office` 仍不在 `PLUGINS` 白名单（唯一未进包插件，结论仍成立）
- 本轮改动明细：27 个在清单内 + 33 个不在清单内

### 方法备忘

- 快照校验脚本是**只读**的，沙箱内可直接跑，不受 `NODE_OPTIONS` shim 陷阱影响（该陷阱只影响会 `fs.rm` 的测试）
- 用 Python 解析 `.github/windows-native-package-inputs.json` 与 `git status` 求交集，是定位快照漂移最快的方法

---

## 评估 09-15~09-16 这轮「质量改进」（用户提问：改进质量如何 + 项目还缺什么）

**方法**：只读取证 + 用 PyMuPDF 渲染真实产物目视 + **三路独立调查**（两路独立评估 + 一路「事实终审」）。事实终审被明确要求**尽力证伪**，结果**纠正了我自己 4 处夸大**——这个方法有效，值得复用。

### 结论：过程质量高，效果质量未证明

| 维度 | 评级 | 关键事实 |
|---|---|---|
| 文档诚实度 | A | 主动记录失实（"各有独立页"不符、雪过度概括、Pro 审稿漏检），我核对**全部属实** |
| 评测方法论 | A− | 留出集意识、禁止自评晋升；**但 46 案断言 100% 是 `llm-rubric` 模型裁判** |
| 注入落地 | B+ | 真注入（`work-quality.mjs:6-11` + hash 断言）；**但 `test-profile-skills.mjs` 不在 `npm run check`** |
| 工具能力改进 | B+ | `process-layout.mjs` 流程图版式真实有效（目视确认优于表格）；硬限 3-4 步、坐标硬编码、`loopLabel` 可能与回环线重叠 |
| 提示词改进 | B | 消除的是真矛盾（最低字数/强制返工/断链规范） |
| 效果证据 | **D+** | **真实端到端 n=3，三次 `input.txt` 逐字相同（同一"水循环"题）**，无对照 |
| 成品质量 | **D+** | 5 页零插图、正文只占上半页、第 5 页活动答非所问 |

### 🔴 最重要的三条长期事实

1. **核心瓶颈是渲染工具表达力，不是提示词。** `plugin.mjs:155` 自认"不支持图片、自由坐标、字体、动画"；`index.mjs:717` **硬断言 PDF `imageCount` 必须为 0**；PyMuPDF 实测 10/10 页 `imgs=0`。项目自己的 `yan-agent-research.md:52` 也说"工具表达力不足无法用更严提示词补齐"——**但投入仍偏向提示词**。
2. **视觉自检在用户机上不可用（受控降级）。** `render.mjs:437-440` 找不到 soffice 就抛错；打包链（`build.files` / `prepare-mochi-resources.cjs`）**不含 LibreOffice**；开发机有 `/Applications/LibreOffice.app` 才能跑。**注意口径**：`visuals-oss-research.md:21` 的 G2 是**候选库选型门槛**、不是全局禁令，且 PPTX 由 pptxgenjs 生成不依赖它 → 说"自我矛盾/功能崩溃"过重，准确说法是**本轮最核心卖点在目标环境默认不成立**。
3. **无可核实的真实教师反馈。** `参赛材料/媒体/`、`验收证据/` 目录为空；签字表全"待补"；`build-standard.md:116` 的"老师反馈"是作者叙述。整套评测是"自己出题自己评分"。

### 其他实质缺口

- 无字号/位置控制 → "大标题+小正文"失衡与留白**无法在生成侧修复**
- **Windows 安装包停在 09-10**：09-12 主题改造、09-16 提示词改进**都没进包**
- 安装无签名/无公证（mac `identity:null`、win `verifyUpdateCodeSignature:false`）
- `mochi-approval/test.mjs`（**审批写操作**）不在任何 CI 步骤
- 5 页 PDF 达 6.6 MB（每页各嵌一份 Noto Sans SC 子集，10 个子集）

### 已验证的正面事实（不要低估）

- **第 3 页的"定页修订"确实只动了那一页**：v1/v2 的 p1/p2/p4/p5 字节完全一致（30804/50190/49759/50779）
- **流程图版式是真实进步**：三卡片 + 循环返回线 + 诚实标注"这是一条简化的路径"
- 修订第 3 页属"用内容准确性换版面改善"（删了物态标注、加了与标题"三站"矛盾的"循环"行，但填满了留白）

### 报告产出

`output/improvement-eval/改进质量评估.md`（含渲染对比图 v1-p3/v2-p3、proc-p4 等）
