---
name: teaching-material-find
description: 帮教师从本地工作区和已核验的资料来源寻找、筛选并整理教学材料；用户说找课件、找教案、找素材或整理资料时使用。
whenToUse: 用户需要为课程、班会或教研寻找已有文件、图片、文档、课件或外部权威资料时使用。
user-invocable: true
---

# 教学材料查找

## 资料边界

先读同目录的 `teacher-resources.json`。它只记录来源、版本待确认项和许可边界，不包含教材全文，也不能代替具体页面的授权核验。

- 高中资料默认优先人教版；英语优先外研版。外研版英语的具体版本和册次仍须教师确认，不能自行补全。
- `textbook-library` 只能作教材目录索引：其代码的 MIT 许可不覆盖教材正文。
- OpenStax Biology 2e 仅作英文生物补充参考，许可为 `CC BY-NC-SA 4.0`；使用前保留署名并核对非商业、相同方式共享等条件。
- 不下载版权不明资源，不把搜索摘要写成已读全文，不把链接存在说成“教材全文知识库已经建好”。

## 隐私门禁

外网检索前，只保留学科、年级、知识点和用途等非个人条件。学生姓名、学号、联系方式、健康、考勤、成绩、处分、家庭信息，以及可识别的附件内容都不得进入外网 query 或上传到外部资料站。

若老师的问题同时包含个人资料和通用教学主题，先用授权校园/本地工具处理个人资料；只有能明确剥离后的通用主题，才可单独查外部资料。网络资料不是本班学情证据。

## 工作法

1. 从课程主题、年级、用途和文件类型提取检索条件；信息足够时不要让老师填复杂表单。
2. 先调用 `file.search` 查当前工作区与允许目录。命中后用 `file.read` 或对应的 `ppt.inspect`、`doc.read`、`pdf.read` 获取结构化摘要。
3. 本地不足时才调用原生 Web 搜索。先查看返回 `content` 中的范围说明：
   - 写明“经可信来源域名校验的 SearXNG 结果”时，只使用返回的来源链接；不要用 `site:` 查询语法代替来源校验。
   - 写明“中文维基百科”或“配置的百科检索接口”时，结果只可作百科参考，不能当课程标准、教材正文或权威教辅依据。
   - 未配置或失败时，说明实际覆盖的本地范围，不编造外部结果。
4. 需要打开网页时，只访问资料目录中列出的权威域名，或本次检索已经校验的来源 URL。网页未能抓取正文时写“仅根据检索摘要判断”。
5. 按“可直接使用、需要改编、仅作参考”三档返回 3–6 个高相关结果；每项说明适用年级、用途、来源和许可/待确认点。
6. 用户要求整理文件时，先展示拟创建的目录与移动清单；`file.create_folder`、`file.copy`、`file.move`、`file.rename` 按当前审批策略执行。

## 能力缺口

缺少 `file.*` 或 Web 能力时，说明实际搜索过的范围与未覆盖范围，不编造材料存在。外网搜索适配器本身无法判断任意 query 是否含个人资料；调用前的隐私门禁仍是必须执行的规则。
