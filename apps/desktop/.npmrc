# Electron 二进制默认从 GitHub Releases 下载，国内直连经常 ETIMEDOUT。
# 这里切到 npmmirror 镜像；如果人在境外，把下面一行删掉即可。
electron_mirror=https://npmmirror.com/mirrors/electron/

# 同理，electron-builder 打包时也要下 Electron 与签名工具链
electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/

# 校园数据不出校：不发送任何审计数据
audit=false
fund=false
