# MOCHI-P1-DOCTOR-UI-01

执行中，Lagrange/terra-max唯一owner main.ts、新doctor-window.ts、必要窗口测试；冻结已验doctor.ts，不改Chat×Work/preload/renderer/profile/锁。STARTUP01行为保留。alpha目录包已完成，后续主进程顺序集成不覆盖本次证据。

依据方案§22/23：native菜单医生窗口，11项真实状态/中文动作、重跑、复制、保存白名单脱敏报告，关闭取消资源；复用已验Electron/Node接口与P1生态检索，无新框架。读取现有profile/env非秘密配置，不能直接读取.credentials.yaml/校园session或另建credential watcher。无配置的项明确unavailable而非PASS。

并行只读核固定alpha live ctx.credentials到主进程的窄鉴权结果bridge，复用现有api-gateway/typert和本地插件注册习惯，给具体接口/鉴权调用路径，暂不写plugin/profile。bridge是完整医生密钥验证的剩余项，不因当前窗口可运行而称全部完成。原生窗口实施不等待bridge。真实窗口测试重跑/导出脱敏/关闭清理，交最小diff与证据供root复核。
