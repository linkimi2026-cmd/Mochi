# 🔴 Mochi 紧急时间表（截止提前版）· 2026-09-11

> **status**: superseded（排期已走完）　**last_verified**: 2026-09-12　**verified_by**: 工具线
>
> **权威范围（2026-09-12 裁定，见 `docs/DOC-AUTHORITY.md` §1）**：
> 本文是 **L0-A 排期与交付判据事实源**——管**出包/演示截止日、URGENT 编号、砍项清单、完成判据**。
> **不管**产品功能设计（归 `Mochi-总体方案.md`，L1）、**不管**打包命令路径（归 `docs/build-standard.md`，L0-B）。
> 凡文档与本文在**排期/判据**上冲突时，一律以本表为准。
> ⚠️ 本文的**交付目标已于 2026-09-11 达成**（`release/2026-09-09`、`release/2026-09-10`）——**但「达成」仅指两个平台的安装包已存在**：URGENT-03 的判据（全新机双击验收）**从未做过**，两个包也都是 **20 插件旧包**，不含 9/12 的新能力。口径详见 `docs/DELIVERY-LEDGER.md`；
> 后续排期需另建新表，不再沿用本文的时间盒。

> 建立：2026-09-08 22:1x
> 触发：**外部把时间提前了**，原按 2026-09-30 倒排的所有时间表作废。
> **新截止：2026-09-11（周五）**，硬卡点：9/10（周四）晚必须出可演示成品，9/11 留给验收与补拍。
> 与 `Mochi-方案总纲.md` §九、`plan/03_RUNTIME_AND_PLAN.md` §9.2、
> `1.5阶段任务/README.md`、`Mochi-总体方案.md` §45 冲突时，**一律以本表为准**。
> **作废标注兑现状态（2026-09-12 复核）**：`1.5阶段任务/README.md` ✅ 头部横幅已加；
> `Mochi-方案总纲.md` ✅、`plan/03_RUNTIME_AND_PLAN.md` ✅、`Mochi-总体方案.md` ✅ —— 三份**已由本轮补加**
> 头部作废/范围横幅（此前只埋在正文中段）。

---

## 0. 用户裁定（原文口径，最高优先级）

- 「紧急事件在工程项目中明确标注，我们要在这周五之内完成。」
- 「之前那个时间表有问题，他们把时间提前了。」
- 外部截止 = **2026-09-11（本周五）**。
- **（2026-09-08 22:2x 追加）「只弄演示的可运行的桌面 APP，其他的不管。」**
  → **本轮唯一交付物 = 一台能双击打开、能现场演示的 Mochi 桌面 APP。**
  → 答辩演练 / 演示录屏 / 申报材料 / Win 包 / 教室端 / LAN / 记忆 UI **全部砍出本轮**（见 §4）。
  → 判据收窄为一句话：**双击图标 → APP 起来 → 现场跑通演示场景，不出终端、不出英文报错。**

---

## 1. 紧急标注约定（全项目统一，写进每张表）

| 标记 | 含义 | 处置 |
|---|---|---|
| 🔴 **URGENT** | 不做则 9/11 交付不成立 | 三天内必须清，见 §3 倒排 |
| 🟠 HIGH | 影响演示观感/评委第一印象 | 有余力必做 |
| 🟡 NICE | 锦上添花 | 只在 🔴🟠 全清后做 |
| ⚫ **CUT** | **本次明确砍掉** | 写在这里是为了防止临时又捡起来；答辩时作为"下一步"讲 |

**规则：任何文档、工单、看板里出现"紧急/加急/urgent"字样，必须挂 URGENT-xx 编号 + 判据 + 负责人。
没有编号的"紧急"不算数。**

---

## 2. 🔴 紧急事件清单（URGENT-01 ~ URGENT-06）

> **范围已收窄（2026-09-08 22:2x 用户裁定）：只做可运行的桌面 APP。**
> 原 URGENT-05 答辩演练、06 录屏、07 品牌终检、08 材料齐套 —— **除品牌终检并入 URGENT-06 外，其余全部移入 §4 砍项**。

| 编号 | 紧急事件 | 判据（怎么算完成） | 现状实测（2026-09-08 22:1x） | 归属线 |
|---|---|---|---|---|
| **URGENT-01** | （**2026-09-08 时点**，现已过时——白名单最终开到 **26**）打包白名单 12 → **16** + 断言同步 | `prepare-mochi-resources.cjs` 含 grades/presentations/modeling/memory；`test-package-resources.mjs` 计数断言同步；测试通过 | 🔴 **未做**。白名单实为 12，runtime-profile 已 16，差 4 个插件 | 本线（立即开工） |
| **URGENT-02** | cwd 修正 + 环境变量注入 | `electron/dsh/web-host.ts` cwd 不为 `/`；`MOCHI_CAMPUS_API_URL` / `MOCHI_SEARXNG_ENDPOINT` 有注入 | ⚠️ 待复核（1.5 阶段列为 🔴 阻断，未见回执） | 本线（立即开工） |
| **URGENT-03** | 重新打包 dmg + **双击验收** | 全新机 U 盘拷入 → 双击安装 → 双击打开 → 不出现终端/英文报错 → 校园功能可用 | 🔴 **双击验收从未做过**；现存产物为 9/5 22:11 旧 `.app`，713MB，dir 格式、无 dmg、adhoc 签名 | 本线 |
| **URGENT-04** | 校园网/云端可达性实测 | 演示现场网络下 mimo 模型 + 校园服务均可达；不通则切本地兜底数据 | 🔴 未实测。不通则演示空转 | 用户确认场地/网络 |
| **URGENT-05** | **演示场景在打包版内跑通** | 传话 / PPT 二次编辑 / 成绩分析 / 教学建模 / 对话内审批 —— 五项在**打包版 APP** 内各跑通一次 | 🔴 从未在打包版内验证过；目前只在 dev 通道（3090）验过 | 本线（9/10） |
| **URGENT-06** | 启动稳定性 + 品牌终检 | 冷启动有实测数字；失败有诊断页不是白屏；界面无"其他名字"口径 | 冷启动零实测；失败诊断页未实现；身份铁律已在 dev 通道落地，**打包产物未终检** | 本线（9/11） |

---

## 3. 三天倒排（9/9 周三 → 9/11 周五）

> 时间盒原则：**每步都有止损线。到期未过 → 立刻降级，不恋战。**

### 9/9（周三）—— 清阻断，出包

| 时段 | 事项 | 止损线 |
|---|---|---|
| 上午 | **URGENT-01** 白名单 12→16 + 断言同步 + 资源测试通过 | 卡住 >2h → 只保 grades/presentations/modeling，memory 降级 |
| 上午 | **URGENT-02** cwd 修正 + env 注入 | — |
| 下午 | **URGENT-03** 打包 dmg（arm64 优先） | 打不出 dmg → 退 dir 格式 `.app` + zip |
| 晚 | **第一次双击验收（本机）** | 打不开就当晚定位到具体报错，9/10 早出结论 |

### 9/10（周四）—— 全新机验收 + 演示场景全过

| 时段 | 事项 | 止损线 |
|---|---|---|
| 上午 | **URGENT-03** 干净用户/全新机双击验收 | 装不上/打不开 → 锁定单一根因，不发散 |
| 下午 | **URGENT-05** 五项演示场景在**打包版 APP** 内各跑通一次 | 挂一项就换备胎 Demo，不当场改架构 |
| 晚 | 修验收与场景暴露的阻断项；出第二版包 | — |

### 9/11（周五）—— 稳定化 + 备用包

| 时段 | 事项 |
|---|---|
| 上午 | **URGENT-06** 冷启动计时 + 失败诊断页 + 品牌/身份终检 |
| 下午 | 出备用包（zip 备胎 + 第二台机器验证）+ 现场预演一遍 |
| — | **URGENT-04** 演示场地网络实测（到现场做，不通则切本地兜底数据） |

---

## 4. ⚫ 本次明确砍掉（答辩时作"下一步"讲）

| 项 | 原计划 | 处置 |
|---|---|---|
| **嘉行联答辩演练（原 URGENT-05）** | 🔴 原"唯一剩余项" | ⚫ **本轮砍**（用户 22:2x 裁定"其他的不管"） |
| **演示录屏/视频（原 URGENT-06）** | 🔴 兜底 | ⚫ **本轮砍**。风险自留：打包彻底失败则无兜底 → 所以 9/11 必须出 zip 备胎包 |
| **申报/答辩材料齐套（原 URGENT-08）** | 🔴 | ⚫ **本轮砍** |
| Windows 打包（P1-5） | 🟠 | ⚫ 本轮只出 mac-arm64 |
| **嘉行联答辩演练（原 URGENT-05）** | 🔴 唯一剩余项 | ⚫ **本轮砍**（用户 22:2x 裁定"其他的不管"） |
| **演示录屏/视频（原 URGENT-06）** | 🔴 兜底 | ⚫ **本轮砍**；风险自留：打包彻底失败则无兜底 → 9/11 必须出 zip 备胎包 |
| **申报/答辩材料齐套（原 URGENT-08）** | 🔴 | ⚫ **本轮砍** |
| Windows 打包（P1-5） | 🟠 | ⚫ 本轮只出 mac-arm64 |
| 零本机依赖四项硬改（B1 soffice / B2 Docker / B3 git / B4 字体） | 🟠 必做 | 校内试用路线下砍，留作下一阶段 |
| 开机自启（A10） | ⚪ 后置 | 砍 |
| 首启向导 + 环境自检（A13） | 🟡 | 砍，只保留失败诊断页 |
| P3 教室端能力 / P4 LAN 直连 | 总体方案 §45 | 全砍，答辩只讲设计 |
| Eval v1（150 条 case） | P1 | 砍到 Golden Demo 人工过 |
| 教学建模第二三项小样 | P4.5 | 只保已完成的圆锥曲线切线一项 |
| 记忆 UI 设置页 | P5 | 砍，对话内 list/forget/clear 已够用 |

---

## 5. ❓ 仍需拍板（阻塞排期精度，1 小时内要给答案）

> 第 1 项已于 2026-09-08 22:2x 拍板（**只做可运行的演示用桌面 APP**），其余三项不阻塞开工。

1. **演示机是哪台**？—— 决定打包架构（arm64 / x64）与要不要出第二份包。默认按本机 mac-arm64 出。
2. **演示现场的网络**？—— 校园网能否连到 mimo 与校园服务（URGENT-04）。不通则演示前切本地兜底数据。
3. **现场演示哪几个场景**？—— 默认五项：传话 / PPT 二次编辑 / 成绩分析 / 教学建模 / 对话内审批。有指定就换。

---

## 6. 状态同步纪律（三条，紧急期内强制执行）

1. **每条 URGENT 状态变化，当天回写本文件 + `WORKLOG.md`**，不接受口头汇报。
2. **测试/验收必须由监制方独立复跑**，信测试不信汇报（沿用工具线既有纪律）。
3. **任何"改了但没验证"的项，一律记为未完成**，不得按完成排后续。


## 7. 2026-09-08 主会话最新直接指令（覆盖旧冲突范围）

用户要求今天完成教师端，外部只余3天；Windows与Mac均要交付。PPT生成必须是真实PPTX，检索GitHub后只采用实际更好的功能/技能，不全面盲升。用户最新授权Chat×Work实现：参考胶囊切换结构，保持Mochi风格，基于真实执行状态自动从聊天切工作并可手动切回。本条覆盖旧文档“Win砍掉”及“UI用户自做不派发”，不恢复无关教室端/LAN/申报工作。root仍负责架构与独立审核，现有terra-max负责实现，干练代码。

学校配置来自用户本轮照片：Windows10教育版20H2，build19042.2965，Intel i7-12700H 2.30GHz，16GB RAM（15.7GB可用），x64，20点触摸。未把照片当目标机安装/网络实测通过；不记录设备ID/产品ID。

URGENT-03升级为Mac arm64安装/运行包与Win x64安装/运行包；URGENT-05包含可编辑PPTX生成/定页修改。新增URGENT-07：Chat×Work胶囊与真实状态自动切换，手动覆盖、会话/输入/产物保持，浏览器验证。

唯一写入分工：Halley desktop打包/运行集成/profile/vendor锁；Lagrange client-plugins/mochi-workbench；Maxwell plugins/mochi-presentations及相关课件skill。其他线当前工作全部保留，先核当前e568c16与未提交差异。root仅写任务/审计文档与运行独立验证，不生产编码。


### URGENT-08：教师→教室一体机 LAN 传话与提醒（本轮直接追加）

用户已明确接收方为教室一体机独立 Agent，并要求校园局域网优先。恢复总体方案§31–37中发现/相识/传话/确认与§27大屏通知这条交付链，不再以旧“LAN砍项”拒绝执行。既有校园relay保留，不让校园云成为同网段消息硬依赖。联动计划仅授权只读找当前入口，未授权改其代码。

必须独立验证：两身份/两数据根相识→精确school/class/endpoint目标→消息落库→教室弹窗→人工已看到回执→教师显示确认；陌生/伪造/错班/解除相识拒绝；相同消息去重、不重复弹窗；断连/ack丢失保持结果不明而非假装未发送。发现广播失败时手动IP明显可用，真实学校网络另待现场测试，不用loopback替代学校实测。

技术校正：§35把超时视作一定FAILED且安全重试不成立；需沿用dispatch既有UNKNOWN与幂等。§34只比较endpointId不能证明设备身份，本轮在不改四层架构的前提下用Node内置crypto签名与首次人工公钥指纹确认绑定，拒绝冒用ID；不引入第二套任务系统或3090认证旁路。

### 后续用户范围与默认模型补充

- 用户明确只做教师端与教室端；停止环境 Doctor 客户端的新增扩展，已有启动/失败页面仅保留必要构建修复，不新增医疗角色客户端。
- 用户要求 aiaaa.cc/v1 的 DeepSeek Expert 视觉模型与 MiMo 共同作为教师未自填 Key 时的默认选项。显示名称已知，准确 model ID、图片协议、实际调用和共享凭据托管尚未验证。密钥不记入本文件、代码仓库或日志；受限属性未确认前不随安装包分发。
- 总体方案允许额度可控、可轮换的受限默认 Key；是否符合此条件待用户确认，不能先强制新增网关。若为管理或未限制凭据，不随包分发。教师自填凭据与默认凭据隔离，不把开发机环境配置误称为所有教师免配置可用。此新增项不暂停 LAN、PPTX、Chat×Work 和双平台打包。
- Chat×Work 采用最小官方公共扩展：header.actions 透传已有 selectedView/selectView，保留官方 header 与会话 store；Lagrange 负责固定源码适配与插件，Halley 唯一负责其构建与分发集成。

### root 本轮独立审计证据

PPT：Node22独立执行 `node --test test/presentation.test.mjs`，换行预算修复后9/9通过；独立生成 `/private/tmp/mochi-root-ppt-review-20260908-a/presentation.pptx` (SHA256 eca25193cf34c5ecd0f36c179eabe6d750ae7874e879944affcbcbc391cff4ac)，同目录PDF/source/manifest齐全。root实际查看Quick Look首页PNG，中文与正文显示正常。此证据只覆盖首页macOS预览，不覆盖所有页或Windows；实现者报告同机LibreOffice26.8导出有字体解析差异，保留限制。代码已冻结交打包，执行者转入LAN。

打包：root读diff发现缺少isAbsolute导入，已派修且当前源码核实导入补齐；跨Office/原29包目标冲突检查已加入。成绩包原会含node_modules.nosync，执行者已加生产files白名单，实际pack dry-run降至5项/61,338 bytes；完整16插件staging仍待实际运行（**2026-09-08 时点**；现为 26 插件，见本文件 URGENT-13）。

新用户要求：四个教师预设纳入今晚交付，票见 MOCHI-TEACHER-PRESETS-01.md；尚未实现/验收，不能以计划记录当作已完成。

### 用户新增：Mochi shell 与浏览器执行

Mochi报告无法跑shell、缺Playwright和Chromium；用户要求补齐。纳入desktop执行/打包负责人Halley范围，先复现实际宿主沙箱错误，区分依赖缺失、沙箱初始化、审批与工作目录限制，不以本机Codex shell能运行代替Mochi工具可运行。Windows核pwsh与ACL runner，Mac核Seatbelt/sandbox-exec。

复用官方Playwright固定版本和对应Chromium，选择受管、可迁移路径；Node包与浏览器二进制均须从实际Mochi执行链启动。检验本地测试页读取与截图、取消/关闭、打包后离开源码运行。核Win10 20H2和Mac系统支持，禁止只加开发依赖/开发机缓存就称交付。用户给出的npm安装命令代表安装目标；若为便携分发选择等价官方安装路径需记录。此项尚未复现/实现/验收。

### 最新目标补充：搜索、教材、主动记忆、图标

- 教材覆盖高中三个年级全部；化学/生物/语文人教，数学人教A，英语外研社，物理教科。已核33册候选目录（数学有分片），见MOCHI-TEXTBOOK-SOURCES-01.md。GitHub未声明许可，目录存在不等于教材开放授权；尚未核版权页年份/文本质量/实际入库。保留本机导入与来源/页码/版本索引，不向公开仓库提交教材全文。
- 网络搜索需复现实际mochi-web-search调用；当前SearXNG为可选未设，学术/百科结果不能冒称全网检索。修复后验证真实中文教学查询、可点击来源、超时/无结果，不无声编造搜索成功。
- 更主动记忆复用现有mochi-memory长期/工作记忆底座：主动召回已存相关偏好、任务延续；对明确长期偏好/重复稳定习惯可提炼记忆，保留来源、查看/纠错/清空。不得把全部聊天、学生成绩明细、凭据或临时一句话自动写成长期事实。具体实现尚待派发，不把现有六个工具等同主动记忆已完成。
- App图标沿用用户已给Mochi品牌样式，核可用单图标资源；不可把整个品牌说明sheet当应用图标。Halley纳入最终打包资源。
- root对修后实际staged PPT `test-plugin.mjs` 重跑ALL GREEN（session17054），原schema阻断已闭环。

root搜索实证：Node22直接调用当前FreeWebSearchProvider默认无SearXNG配置，query=“人教版 高中 化学 化学平衡 教学”、maxResults=4，耗时10025ms，返回OpenAlex/Crossref的日文生态报告、高校思政/高职语文/高职数学等不相关结果（session78219 exit0）。这是功能质量失败，不是没有联网；修复验收必须覆盖真实中文教学相关性、全网来源和耗时，不能以非空响应为通过。

用户再强调自主发现其他Mochi：LAN持续发现/TTL上线离线必须默认工作；模型也需要只读设备名单工具知道候选与已配对对象，自动发现不等于自动授权发送。已派Maxwell服务/工具、Lagrange UI，尚待双端验收。

---

## 本轮追加（2026-09-12 夜）：URGENT-09 ~ URGENT-13

> 触发：主理人一次性下了 8 条要求，累计成以下五项。**判据写在这里，完成状态以本条为准**，
> 不以「计划里写了」当已完成。

| 编号 | 事 | 完成判据（可复现） | 本轮状态 | 归属 |
| --- | --- | --- | --- | --- |
| **URGENT-09** | 提示词 V1.3 落地 + 模型无关 | `Mochi_系统提示词_V1.3_完整阅读版.md` 无任何模型/公司名；`core.patch.yml` persona 同步且 YAML 可解析、≤3400 字符 | ✅ 完成。persona 2763 → 3238 字符；新增 §12 接材料 / §13 交东西 / §14 审美与教学递进 | 工具线 |
| **URGENT-10** | 对话界面 / 工作界面分离 | 对话界面工具面只剩 `mochi_request_work_mode`（`restrict` 生效，可测）；工作界面 = 现对话界面全量工具 + 工作台；**同一会话**内切换；一上来点「工作」不弹窗；无审批通道时**不收窄**（防呆） | ✅ 完成。mochi-modes 16/16、客户端 8/8、workbench 13/13，含两组负向对照（关掉 restrict → 4/16；工作台总渲染 → 12/13） | 工具线 |
| **URGENT-11** | 16 个缺失工具全部实现 | `file_*`(6) / `doc_read`·`doc_create`·`doc_edit`·`doc_export` / `pdf_read` / `ppt_inspect` / `spreadsheet_*`(4) 在打包产物里能被 `test-package-resources` 逐个断言到 | 🟡 **当时完成，现已回归**（2026-09-12 19:4x：`test-package-resources` FAIL，`mochi-presentations` 的 `drawTextLine` 导入找不到导出）。mochi-files 22/22、mochi-documents 30/30、mochi-sheets 22/22、mochi-presentations 17/17；打包期工具注册面已钉死 | 工具线 |
| **URGENT-12** | 图片编辑 + 自动绘制 + 教材配图 | 4 个工具真实可用：`image_find` / `image_edit`（真像素、不覆盖原图）/ `diagram_draw`（真坐标轴刻度图例，SVG+PNG）/ `teaching_image_match`（只读本地教材索引，无匹配就说无匹配） | ✅ 完成。34/34。**零新增运行时依赖**（复用闭包内 `@napi-rs/canvas`）。调研见 `docs/visuals-oss-research.md` | 工具线 |
| **URGENT-13** | 一切修复必须进安装包 + 出包标准化 | `PLUGINS` 白名单登记全部新文件；`test-package-resources` 过；快照清单 0 漂移、字节差 ±0；`docs/build-standard.md` 写明唯一路径 | 🟡 **当时完成，现已回归**（2026-09-12 19:4x 复验）：白名单 21 → **26** 插件、快照 461 → 491 均属实；但 ① `test-package-resources` 现为 **FAIL**（`mochi-presentations` 导入 `@mochi/pdf-layout` 的 `drawTextLine`，钉的 tgz 无该导出）；② 快照清单现为 **11 个文件不一致 / 字节差 +34,614**。详见 `docs/AUDIT-CODE-2026-09-12.md` 缺陷 1 | 工具线 |

### 本轮新增的插件（都在打包白名单里）

`mochi-files`、`mochi-sheets`、`mochi-visuals`、`mochi-modes`、`mochi-modes-client`；
`mochi-documents` 补 `document-io.mjs`。

### 本轮新增的文档 / 脚本

- `docs/visuals-oss-research.md` —— 图片与图表方案的 GitHub 调研（含「不用/为什么不用」）
- `scripts/reconcile-snapshot-manifest.mjs` —— 快照清单收敛工具（重算哈希 + 自动登记新插件文件）
- `docs/build-standard.md` §4.6 —— CI 覆盖不到的测试清单（必须本机跑）

### 仍未闭环（诚实列出）

1. **真机视觉验收未做**：本机 `apps/desktop/node_modules/fs-ext` 是 x86_64、arm64 `dlopen` 失败，
   宿主起不来 → 对话/工作界面的**实际渲染**（模式条小字位置、工作台显隐）只有静态证据。
   必须在真机上过一眼。
2. **CI 覆盖缺口**：公开 CI 不装依赖，凡 `import '@deepseek-ai/dsh-tools'` 的测试都跑不了
   （mochi-modes / mochi-visuals / mochi-sheets / mochi-documents / mochi-presentations 的绝大多数）。
   本轮只把两条真正零依赖的加进了 CI（`plugins/mochi-files/test/paths.test.mjs`、
   `client-plugins/mochi-modes/test/client.test.mjs`）。补法 = 给 CI 放 dsh-tools 测试替身。
3. **`teaching_image_match` 不返回图片本体**，只给取图方式；「一次匹配 → 一次取图」没有端到端跑过。
4. **`mochi-visuals` 只在 macOS arm64 验过**；Windows 上 `fonts.mjs` 的字体候选路径必须复验，
   否则中文图表会出豆腐块。
5. **待用户拍板**：V1.3 英文版 docx 里有一条「高中试题默认固定为新高考Ⅱ卷」，
   与现有「老师说的 > 校本模板 > 已核验档案」冲突，**没有写进去**，等拍板。
