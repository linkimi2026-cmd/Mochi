# MOCHI-P0-ALPHA-NATIVE-02

P0 §6/§45 新patch运行树的Electron原生绑定；root审计，唯一实现者复用 p0_packaging_audit/terra-max（followup_task已发，需回报角色状态）。新spawn因thread limit失败，未创建p0_native_rebind，不能称其在运行。

新只读输入 /private/tmp/mochi-alpha-runtime02-moved.92OFPB（直接consumer根），lock5f9e0a95263e07f7d6b49c37a5a45441a156dc7e6a2f951720a3d6e20cdb252a，llm-deepseek JS9cc4dce9a411f1f1ae7e7985babf826b3179abcf39b3464c9d13b79b024d5130。root独立普通NodeCLI/28562文件字节/12内部links已验，不能沿用旧NATIVE01代替新输入。

复用 NATIVE01 固定官方MIT electron/rebuild3.6.1与GitHub复用记录、全部旧脚本/harness（alpha-native/20260906T214431Z-native01），唯一新外部普通copy与alpha-native02/<timestamp> evidence。Maxwell/Lagrange并行只读新consumer，不改他们输入；不写production/UI/原锁/旧候选/真实HOME/联动计划。普通rsync -a不含-H，不加无效--no-H。

env-i新HOME/DSH/XDG/TMP；普通Node22.22.2调用固定rebuild onlyModules fs-ext/buildFromSource，目标Electron39.8.10/darwinarm64/ABI140，一次。实际Mochi runner ELECTRON_RUN_AS_NODE=1且子进程继承，验证真实session-persistence-jsonl owner加载、跨进程flock EAGAIN/释放后成功/清理、koffi、CLIalpha。无forceABI/install/升级/其他native重建，无GUI/Windows/模型范围。

仅允许fs-ext/build/**与固定工具生成bin/darwin-arm64-140/**变动。node-gyp python3 helper精确旧cleanup预先授权：before不存在、目录仅此symlink、raw目标恰CLT python3后仅unlink链接自身及空目录，保留清理前失败，不碰目标；其他外逃/差异回报。绝对日志/status路径，正常参数准备失败可在scope内纠正留证，不重复求确认。

交复用脚本/实际命令/句柄/exits/hash/无共享inode/闭包/非目标无变/lock不变与简短报告；root独立复验才放行。模型/角色需保持terra-max，不可静默更换执行者模型。


## 2026-09-07 root 收口

已验收 / PASS（当前 patched consumer 的本机 Electron ABI140 范围）。root 独立运行真实 flock 排他/释放/清理及闭包检查通过，已审阅最终 CLI 与输入不变证据。不代表 Windows 或完整 profile。
