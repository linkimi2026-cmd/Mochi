# PPT 历史取材证据

本目录保存旧版演示录屏截图和当时的自动化操作脚本，只用于追溯画面来源，不是当前答辩的执行入口。

历史脚本中的演示密码已替换为 `REDACTED_DEMO_PASSWORD`，因此不能直接运行。需要重新采集时，应从本机受控凭据读取登录信息，禁止把账号密码写回脚本、PPT、截图说明或 Git。

当前答辩请从 `../Mochi四分钟答辩/` 开始；评委成品位于项目根目录的 `01-Mochi-参赛交付包-2026-09-14/06-答辩PPT/`。
