# Mochi 现行总体方案

> **status**: active
> **last_verified**: 2026-09-13
> **verified_by**: Codex（以当前源码和配置为主；现场状态另见 `docs/PROJECT-STATUS.md`）

## 1. 产品定位

Mochi 是面向教师办公和校园协作的 AI Agent。它以对话为入口，以可执行工具、工作区文件和校园业务连接为能力基础，让教师从一句需求开始，得到可以继续编辑、演示或交付的成品。

产品重点是以下闭环是否成立：

> 理解需求 → 选择工具和 Agent → 执行任务 → 展示过程 → 产出文件 → 必要时连接校园角色继续协作

## 2. 面向的用户

- 教师：备课、课件、试卷、材料整理、数据分析和日常班级工作。
- 班主任及校园角色：查找学生、处理协作消息、发起或批准受控操作。
- 教室一体机使用者：在受控的课堂角色配置中快速演示和执行任务。
- 管理与评审人员：通过真实界面和真实产物判断系统能力、可用性和治理边界。

## 3. 交互主线

### 3.1 进入工作区

首次使用选择工作区。需要完整演示文件能力时，选择“全部允许所有权限访问”。工作区授权决定 Mochi 可以读取或写入哪些本地文件，不等同于校园账号登录。

### 3.2 切换工作模式

默认聊天模式用于问答。课件、文档、建模、表格和其他需要工具执行的任务，应先切换到工作模式。工作台只在需要查看过程或结果时打开，用完及时关闭，避免遮挡主界面。

### 3.3 发送需求并执行

用户输入需求后发送。界面应明确呈现正在分析、调用能力、生成和完成的状态。结果不能只停留在文字说明；执行型任务应生成真实文件或可交互产物，并支持从侧栏打开。

### 3.4 继续协作

一个任务的结果可以成为下一个 Agent 或校园角色的输入。A2A 用于在能力和角色之间传递结构化任务、状态与产物，让备课、查询、沟通和校园操作形成连续故事线。

## 4. 当前能力

| 能力 | 当前用途 | 主要实现位置 |
|---|---|---|
| 课件与演示 | 新建课件、组织结构、生成可展示 PPT | `mochi-presentations`、`classroom-deck` |
| 文档与试卷 | 生成和整理 Word、练习、说明材料 | `mochi-documents`、`mochi-office` |
| 表格与数据 | 读取、整理和生成表格数据 | `mochi-sheets`、`mochi-grades` |
| 文件工作区 | 读取、写入、打开和管理工作区文件 | `mochi-files`、`mochi-workbench` |
| 视觉与建模 | 生成视觉内容、处理图片、创建模型产物 | `mochi-visuals`、`mochi-modeling` |
| 知识与记忆 | 导入资料、检索知识、保留受控工作记忆 | `mochi-knowledge`、`mochi-memory` |
| 联网搜索 | 使用配置的搜索服务获取外部信息 | `mochi-web-search` |
| 校园连接 | 学生查询、位置、协作消息和受控业务操作 | `mochi-campus`、`jxl-campus` |
| Agent 协同 | 调度、A2A 任务传递、定时任务和局域网连接 | `mochi-dispatch`、`mochi-task-scheduler`、`mochi-lan` |
| 模式与预设 | 聊天/工作模式、教师 Agent 预设、模型预设 | `mochi-modes`、`teacher-agent-presets`、`mochi-model-presets` |

仓库还提供班会准备、课堂课件、学生跟进、学生出入申请、教师日报、教学资料搜索和周报等技能。插件存在说明源码具备相应入口；特定机器和线上服务是否可用仍按项目现状和验收记录判断。

## 5. Agent to Agent

A2A 的目标是让不同 Agent 和校园角色共同完成一件事，而不是在界面上罗列功能。例如：

1. 教师让 Mochi 生成一份课件；
2. 资料 Agent 搜索并整理材料；
3. 演示 Agent 生成可编辑的 PPT；
4. 校园连接查询相关学生或班级信息；
5. 需要业务动作时，把请求交给有权限的角色审批；
6. Mochi 汇总状态和产物，返回同一工作区。

系统应保留任务来源、目标、状态、产物和审批结果，避免各 Agent 之间只传递不可验证的自然语言结论。

## 6. 角色和权限

- `teacher` 角色允许使用教师预设、已安装预设和用户预设。
- `classroom` 角色使用受控的课堂预设和更窄的插件集合，不自动加载用户预设。
- 工作模式限制在相关通道不可用时可以降级，以避免界面完全不可用。
- 校园写操作始终单独授权；模式限制降级不会放开校园写权限。
- 清除记忆不等于删除审计快照。界面和文档必须准确说明删除范围。

## 7. 模型与服务

新安装的当前默认种子为 `mochi-aiaaa / deepseek-v4.1-flash`，支持文字和图像输入。运行配置还保留可选模型插件，已有用户配置不一定被默认种子覆盖。

校园 API 和 SearXNG 搜索端点没有在运行配置中写死默认值。部署或演示前应显式配置真实服务，不能把历史地址或本地副本当成当前线上状态。

### 7.1 与“联动计划”的关系

Mochi 是教师侧 Agent、桌面工作区和能力编排层；“联动计划”是独立的校园业务、账号权限、数据和服务部署项目。Mochi 通过 `jxl-campus` 显示校园界面，通过 `mochi-campus` 调用校园能力，并把请求代理到显式配置的校园 API。

桌面打包只带审核后的校园静态客户端，不带“联动计划”源码、数据库、服务端密钥或部署环境。安装 Mochi 不会自动部署校园服务。完整边界、路径优先级和演变过程见[项目演变与“联动计划”关系](docs/PROJECT-HISTORY.md)。

## 8. 桌面与 Web 形态

- 桌面端使用 Electron，负责本地工作区、系统权限、托盘、原生窗口和打包。
- Web 界面承载核心对话、插件和工作台交互。
- 两种形态共享核心能力，但文件权限、进程生命周期和部署方式不同。
- Windows 正式包由原生 Windows 环境或 CI 生成；Mac arm64 和 x64 构建要求宿主架构与目标匹配。

## 9. 产品表达原则

产品演示和宣传必须以真实界面、真实输入、真实执行过程和真实产物为主。镜头可以通过鼠标轨迹、聚焦、连续转场、动态排版和节奏变化串联功能，但不应让装饰遮挡界面或制造不存在的能力。

展示重点包括：

- 输入、点击、发送和生成状态完整可见；
- 课件、试卷、建模和网页等成品可以动态打开和浏览；
- 查找学生、沟通老师、校园连接和批准放行形成一条业务故事线；
- Agent 预设通过弹出、滚动或连续切换展示；
- 字幕准确、无乱码、画面清晰、转场连续，工作台及时关闭；
- 宣传片技术规格与最终验收结论登记到交付台账。

## 10. 当前边界

本方案描述当前产品和代码结构，不宣称每项能力都已经在所有设备、所有网络和所有账号下完成验收。Windows 一体机功能正常是本次会话中的用户确认；校园服务器报告、线上部署、视频速度反馈和最终参赛材料状态见[项目现状](docs/PROJECT-STATUS.md)。
