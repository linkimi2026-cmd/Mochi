# Mochi 总体方案（最终整合版）

> **status**: active　**last_verified**: 2026-09-12　**verified_by**: 工具线
>
> **权威范围（2026-09-12 裁定，见 `docs/DOC-AUTHORITY.md`）**：本文是 **L1 产品设计事实源**——
> 管产品定位、功能设计、章节编号、术语表。**不管排期**（排期归 `docs/tasks/MOCHI-URGENT-REPLAN-2026-09-11.md`，L0-A），
> **不管出包路径**（归 `docs/build-standard.md`，L0-B），**不管当前实现状态**（以源码为准，L2）。
> 本文此前自称的「唯一事实源」指的就是 L1 这一层，不是全仓唯一。
>
> **版本**：v2.0 · 2026-09-06（合并《最终能力与功能总纲》：新增教学建模、岗位能力、学生端终态、视觉体系；含实施督导审校修订与 MIMO 主力裁定）
> **硬约束**：本文档只规划，不含实现代码；插件实现由后续专人按本文执行。
> **日期口径**：**2026-09-30** 是**嘉行联参赛材料**截止日（另一条线）；
> Mochi 出包/演示截止已由 2026-09-08 裁定**提前到 2026-09-11**，并**已交付**（`release/2026-09-09`、`release/2026-09-10`）。

---

## 目录

- 第一部分 · 定位与边界（第 1–5 章）
- 第二部分 · 架构地基（第 6–9.5 章）
- 第三部分 · 教师工作 Skills 全景（第 10–14 章）
- 第四部分 · 教学建模工作空间（第 15–20 章）
- 第五部分 · 桌面端原生化：双击即用（第 21–26 章）
- 第六部分 · 教室端能力（第 27–30 章）
- 第七部分 · 局域网设备直连通信方案（第 31–37 章）
- 第八部分 · 记忆系统（第 38–40 章）
- 第九部分 · 岗位、学生端与成果共享——终态视野（第 41–43 章）
- 第十部分 · 下一阶段工作计划（第 44–48 章）
- 附录 A 文档来源对照 / B 待确认清单 / C 术语表

---

# 第一部分 · 定位与边界

## 1. 产品定位

- **一句话**：Mochi 是专注校园工作的 AI 智能体——老师的办公助手 + 教室的大屏助手。
- **与嘉行联的分工**：嘉行联负责连接校园中的**状态**（请假/就医/考勤流转）；Mochi 负责连接校园中的**人、Agent 和任务**。保留现有校园平台，复用经过核验的真实 Harness 能力，不另造一个只有聊天外观的"轻量替代品"。
- **产品层全貌**（同一个共同基础设施支撑，不为每个部门重新创建一套聊天系统）：

| 产品层 | 主要职责 |
|---|---|
| 嘉行联 Campus | 保存校园业务事实，完成学生流动、医务、宿舍、消息与权限闭环 |
| Mochi 工作环境 | 理解目标、讨论需求、调用工具、检查成果、继续修改 |
| 教学建模工作空间 | 创建和使用数学、物理等学科的可交互教学模型（第四部分） |
| 岗位工作空间 | 教师、教务、德育、后勤等岗位的专用操作界面（第 41 章） |
| Mochi 协作网络 | 找到有能力和权限的承接方，派发任务、等待审批、返回成果 |
| 共同基础设施 | 身份、插件、Skills、记忆、成果管理、执行记录、同步与安全 |

- **不是什么**：不是聊天机器人集合，不是监控工具，不是又一个要老师学半天的系统。所有交互在各自 Mochi 对话框里完成（对话即界面）。
- **最终产品判断（体验验收标准）**：让老师觉得"它越来越懂我"，而不是"它保存了很多数据"；"我告诉它我要什么，它去把事情办完"，而不是"它告诉我应该怎么自己去办"；"它自己不会，还能去找别的 Mochi"，而不是"遇到不会的问题就结束"；第一眼觉得"很简单"，用几天以后才发现"原来它背后能做这么多"。
- **三条主线必须连起来**：教学——把抽象知识变成学生能理解的模型；办公——把老师的要求变成真正可用的成果；协作——把跨岗位的事情推进到有依据的完成状态。

## 2. 三端模型（最终定名）

| 端 | 载体 | 使用者 | 本期做不做 | 职责 |
|---|---|---|---|---|
| **教师端** | 老师办公电脑（Mac/Windows 桌面 App） | 老师 | ✅ 本期主线 | 干教师的活：写材料/做课件/管成绩/传话/审批；向教室端发指令与文件；收学生留言 |
| **教室端** | 教室多媒体大屏/一体机（Windows 为主） | 老师（上课）+ 学生（课间） | ✅ 本期主线 | 教室现场助手（独立 Agent）：打开软件/文件投屏、课前预启动、学生问答、大屏通知点名、教学模型课堂模式 |
| **学生端** | 独立学生设备 | 学生 | ⬜ 未来适配（不是砍掉；终态见第 42 章） | 住校生/走读生均不带手机 → 现阶段学生直接用**教室端** |

**两端分工铁律**：教师端的 Mochi 干教师端的活，教室端的 Mochi 干教室端的活；两端通过第七部分的局域网直连方案互通。

**教室端是独立 Agent**（用户 2026-09-06 裁定）：它有自己的模型、自己的身份（如"高二(3)班教室的 Mochi"）、自己的数据根——**不登录任何老师的账号**。两端的关系是 **A2A 协作**（mochi-dispatch 的 ASK/REQUEST 原语），不是"同一账号的两台设备"。它能独立工作：不连任何教师端也能用（学生问答、开软件、大屏通知本机就能做），教室端做的任何事都不依赖教师端在线。

## 3. 学生侧原则：功能受限 · 知识不受限（用户 2026-09-06 裁定，最高优先级）

- **知识不受限**：学生在教室端问任何知识点，Mochi 给完整回答。**不设内容审核闸、不做"受控检索"、不要求老师先过目答案**——否则学生就没东西可问了。
- **功能受限**：限制只在功能面。学生通过教室端的**对话层**直接给教室 Mochi 发消息（无需任何学生账号）。学生角色**可用**：自由问答、查资料、看大屏内容、操作老师发布的教学模型、给老师留言。**不可用**：打开/关闭软件、向任意人发消息、修改设置与偏好、访问老师私人文件、派发任务。
- **防注入策略**：不靠"学生输入不进模型"（已废弃），而靠**工具面收敛**——学生会话只挂只读/问答类工具，不挂执行类工具（开软件、发消息、写文件）。模型说什么管不住，但模型**做什么**被工具清单锁死。剩余风险（被诱导说不当内容）由常规安全护栏 + 教室老师在场兜底。

## 4. 红线与安全底线（不可逾越）

- 预警只能是**流程异常**（如超时未归），禁止疾病预警/风险评分；不做硬件、心理测评、生物识别、GPS。AI 可以整理事实与提出跟进建议，但**不自动认定违纪、不给学生生成"人格风险评分"、不擅自作纪律处分决定**。
- 学生数据最小化：能匿名则匿名，能不落库则不落库。**绝不修改 `/Users/a1379/Documents/联动计划/` 的任何文件**；需要复用的东西复制到 Mochi 再改。
- dsh 遥测永久关闭（`mode: DISABLED` + `DSH_TELEMETRY_DISABLED=1`）。
- Mochi 身份锁死：问名/问司/问模型一律「我是 Mochi，专注校园工作的 AI 智能体」，禁止出现任何底层供应商名称。
- 隐私原则：**记任务不记人、记结果不记轨迹、可查看可一键清空、本地存储**。
- **执行环境安全**：生成的模型配置、表达式、脚本、导入网页和外部文件，都不能获得主应用的全部权限；建模与文档执行环境限制文件访问/网络/运行时长/资源，只通过明确接口交换允许的数据与成果；不直接执行来自题目或网页中的任意代码；教学模型页面不得读取学校登录凭证。
- **计算与界面分离**：数学/物理/排课计算与界面分离；学校业务状态由服务端维护；角色动画不决定状态。重要修改保留审计、版本和幂等标识。
- **验收诚实**：一次成功演示 ≠ 整项能力通过；测试数量 ≠ 任务质量。

## 5. 视觉、插画与动画原则

- **统一设计语言，不统一所有布局**：延续嘉行联品牌/字体/留白/组件/状态语言。教学模型用画布、排课用课表、成绩用核对表、后勤用工单、工作成果用预览区——**不是所有页面都变成聊天气泡**。
- **首页**：保留动态问候（"林老师，早安 / 下午好 / 晚上好"），只突出当下重要事项、继续工作和主要输入入口；高级运行信息按需展开（SIMPLE SURFACE, DEEP CAPABILITY）。
- **插画**：大色块、几何概括、干刷颗粒、清楚的前中后景；先用通用校园示意，之后替换为学校真实素材的同风格插画；**不伪称示意建筑是真实校园，不出现人物**。
- **角色分工**：水母只承担品牌识别；校园猫保持统一角色与逐帧动作（流动叙事及少量反馈），不扩成很多角色、不覆盖专业操作区；Mochi 本尊 = OrbCompanion（球 + 面前一台小电脑）。
- **动画分两类**：产品动画（按压/切换/弹层/列表更新/猫动作，克制与可中断）；教学动画（解释构造、变化和过程，强调暂停、单步、对比与正确性）。**不能为了画风统一，把受力图、坐标轴和几何线条都变成模糊的绘画笔触**。
- **分工**：Image 模型用于原创校园插画、角色和适合的教学情境配图；**精确公式、几何关系、坐标曲线和物理过程由计算与绘图逻辑产生**。

---

# 第二部分 · 架构地基

## 6. 内核与运行形态

- **内核**：dsh（Mochi Harness）源码集成，锁 `dsh-v0.1.3-alpha.1`（commit d347e7039），克隆于 `mochi-harness-src.nosync/`，**不跟 npm latest**（npm 0.1.2-rc.1 更旧）。
- **UI 形态**：官方 Web SPA 嵌 Electron。`apps/desktop/electron/main.ts` 主进程 spawn sidecar（`--port 0` 随机端口 + `--no-open` 硬编码）→ 解析带 `?token=` 的 URL → `loadURL` 进自家窗口。**桌面端天然免登录、不弹浏览器**（开发态已验证；打包态按 P0/P1 验收）。
- **弹浏览器"无法访问"真因**：dsh web 子命令 `openBrowser` 默认 `true`，走脚本/命令入口时浏览器抢跑在服务 ready 之前。桌面壳不受影响；老师永远走桌面 App 入口。
- **profile 体系**：headless / mochi / mochi-web 三 profile 由 `apps/desktop/resources/mochi-web/runtime-profile.json` 声明，生成器 `runtime-profile.cjs` 自动产出三件套（link 依赖 + node_modules symlink + cordis.patch.yml insert）。**无 config 的新插件只改 runtime-profile.json**；需要 config 的插件（如模型路由）走 cordis.patch.yml 手工区完整 insert+config——受管区 insert 不能带 config（大坑 30），禁止手改受管区。
- **数据根**：`~/.mochi-home`（开发态 `.mochi-home.nosync/`），SQLite 用 `node:sqlite`，零外部依赖。
- **运行能力保留并核验**：会话继续、取消、重试、任务恢复、工具调用、技能加载、模型切换、执行记录、文件工作区、子代理和插件管理——不重复造第二套已有能力，也不把"宣传里支持"当成当前项目已经接通。

## 7. 插件体系与对话即界面

- **对话即界面**：传话/任务收发两端全在各自 Mochi 对话框完成，不做独立界面/面板/小组件。审批闸 = 人决定 + 对话内呈现。界面默认只显示"Mochi 正在做什么"（正在找周老师的 Mochi / 正在整理 PPT / 等待对方确认），详细 Tool Call 收进"查看过程"。
- **服务端插件**（`plugins/`，10 个）：mochi-dispatch（任务域）、mochi-campus（校园数据）、mochi-documents / mochi-presentations / mochi-grades / mochi-office（文档/PPT/成绩/Office 预览）、mochi-web-search（免费搜索）、mochi-llm-mimo（第二模型路由）、dsh-better-sidebar（右侧面板）、mochi-hello。
- **客户端插件**（`client-plugins/`，5 个）：jxl-brand / jxl-campus / jxl-theme（嘉行联视觉）、mochi-workbench（教师工作台，侧边栏首位 tab）、mochi-model-presets。
- **右侧面板裁定**：所有面板功能一律 `ctx.betterSidebar.registerTab`，不再用 shell.overlay；老师向 tab 排最前。
- **技能**（`skills/`，7 个）：class-meeting-prep、teacher-daily-brief、student-follow-up、student-movement-request、teaching-material-find、weekly-class-report、mochi。

## 8. 任务域与 A2A（Dispatch Protocol v1 + Mochi Network 终态）

- **四类节点同一 Runtime、不同 Context**：主人端 / 同事端 / 资源端 / 公共端。
- **四原语**：ASK（问一句话等回话）、REQUEST（请求做事等批准）、FIND（找资源）、APPROVE（应答动作）。
- **权限模型**：每个 Mochi 只能在自己主人授权范围内行动；跨人请求必须显式发送；接收方可以拒绝；任何 Agent 不得私自代表另一个人答应。
- **7 态机**：CREATED → DISPATCHING → DELIVERED → COMPLETED / DECLINED / FAILED / EXPIRED。未确认投递结果绝不重发；失败只能经人工确认后建立新 attempt。
- **当前投递通道**：campus relay（服务器只投递一句话，应答永远由人决定）。已通过可靠性验收（`A2A_RELIABILITY_REPORT.md`）；Golden Path 总验收列入 P5。**relay 对服务器的依赖由第七部分 LAN 直连方案解除**。
- **审批 answerer**：`ctx.on` 注册，返回字符串枚举；SDK 通道不下发审批，主交互走 web 通道。
- **协作形态终态（Mochi Network）**：从传话扩展到询问/请求办理/寻找资料/人工批准/成果交付/任务补充/转派/结果验收。每件事明确发起人、主办责任人、协办方、审批人和完成条件。**优先自己完成；确实需要对方能力或权限时才派发**，避免一个普通任务被拆成很多 Agent 空转。
- **能力发现**：先按组织、岗位、权限和可用状态筛选，再进行语义匹配（结构化 capability + LLM 选择；第一版不做复杂向量数据库）。Domain Model 不写死 Teacher-only（未来允许 Student/Medical/Dorm/IT/Academic Affairs/Resource Agent）。
- **必须跑通的代表流程**：找资料（自己找不到→对方 Mochi→对方查授权目录→主人批准→文件返回→继续制作课件或模型）；场地与设备（需求→场地确认→设备保障→变更审批→正式登记→返回结果）；成绩汇报（提交→教务核对→补充分析→生成报告与 PPT→审核发布）；学生事务（请求→负责教师确认→嘉行联记录→目的地确认→返班与异常跟进）。
- **可靠性要求**：已送达、已读、已接单、已批准、已执行、已验收**分开记录**；等待时保存任务、不持续调用模型；对方离线/拒绝/超时/取消/重复提交/服务重启都有明确处理；跨 Agent 不共享密码、不直接开放对方电脑、不把整段私人对话默认转交；已批准的对象/接收者/文件版本改变后重新校验、必要时重新审批；**内部子代理不是另一个老师，不能冒充他人完成授权**；电脑关闭或执行端离线时保留任务并显示等待，不声称仍在处理，恢复后继续不重复执行。

## 9. 模型路由、成本与部署

- **模型主力 = MIMO**（用户 2026-09-06 裁定）：MIMO 更聪明、上下文更长，**日常使用与测试都用 MIMO，GLM 不再用于测试**（`deepseek-official` 仅保留为故障备选路由）。**但 MIMO 要省着用**：常规对话默认"关闭/低"推理档位，重活才开中/高；轻量任务（分类/摘要/状态话术）优先低档位；保持缓存友好结构（见下）；不滥用长上下文。
- **双路由共存**：`mochi-mimo`（默认主力，mimo-v2.5）+ `deepseek-official`（智谱 glm-4-flash / glm-4.7-flash，备选）。模型列表两者同时可选；模型切换后工具和权限契约保持一致，但每个模型仍需单独验证调用与任务表现。
- **推理档位**：中文 UI（关闭/低/中/高/极高），按模型能力声明（MIMO 不声明"极高"），不硬降级——模型没有 max 就没有 max。
- **缓存**：dsh 请求结构已缓存友好（system 无时间戳、tools 顺序稳定、消息 append-only），`cached_tokens` 可监控，无需改代码。
- **成本纪律**：只用免费档/便宜档 API；模型 API、图像生成、存储、分发和应用发布**成本分别记录**；Cloudflare 等云服务按实际服务、额度与计费规则验收，"采用免费计划"不等于"任何规模都永久免费"；**管理密钥不打包进所有人的客户端**（客户端仅可内置额度可控、可轮换的受限默认模型 key）。
- **部署分工**：本地执行 Harness、文档工具、建模与渲染；云端负责必要的身份、任务、审批和协作同步。数学符号计算、视频渲染、复杂解析和求解任务，放到明确配置的本地或独立执行环境，**不默认塞进免费边缘接口**。
- **已知补丁**：`dsh-llm-deepseek` 产物两处补丁（tool_calls 流聚合 acceptIdentity 修复 + 档位中文化），npm 重装会冲掉，重装后必须重打。

## 9.5 校园数据后端、信息导入与文件输入通道（服务器侧，本期必须解决）

**后端现状与缺口**：① 校园数据走本地 wrangler（`127.0.0.1:8787`），只在开发机存在——打包到老师/教室机器即失效，必须云端化为服务器 API（注入缺口见第 24 章）；② 传话/任务当前依赖校园服务器 relay 中转，LAN 直连（第七部分）解除同网段依赖，但跨网段/外网仍需服务器；③ 班级/学生/老师的组织信息没有批量入库通路，Mochi 无法精准识别"哪个班、哪些学生、谁是老师"。**结论：后端躲不掉——组织信息导入、校园数据 API、跨网 relay 三件事都落在服务器上，本期必须解决。**

**组织信息批量导入（管理员用，用户 2026-09-06 新增需求）**：

- 目标：管理员快速把每个班的学生和老师信息**直接导入服务器**；Mochi 精准"认人"（哪个班/哪些学生/谁是班主任/谁是任课老师），老师端选人（发消息/派任务/点名/找人）有准确名录。
- 形态：Excel/CSV 模板（班级-姓名-角色-账号）→ 管理入口上传（或对话框拖入）→ 行级校验（重复/缺列/格式错误给出行号报告）→ 预览确认 → 入库 → 支持增量更新与回滚。
- 要求：快速方便、一次可导入全校、出错可定位到行；名录是 dispatch 选人与同名消歧的基础（第 8 章）。
- **试点**：导入功能完成后，先用用户自己的班试跑一遍，再推广。

**对话框文件输入（两端通用，用户 2026-09-06 新增需求）**：

- 图片/照片/文档可**直接拖入对话框**，也可点输入框"加号"选择文件。
- 这不只是贴附件：拖入即进入 Artifact 流（任务可追踪、产物可继续改，第 10 章七件事）。
- 它是"纸质资料 → 可编辑电子文档"（第 14 章第一批第一件事）的**输入入口**：拍一张纸质表格/试卷拖进去，Mochi 就开始处理。

---

# 第三部分 · 教师工作 Skills 全景

> 来源：《Mochi 教师工作 Skills 清单与推进顺序 V1》+《最终能力与功能总纲》§七。已按仓库真实现状逐条对照，防止"已完成的事被当成新目标重做"。

## 10. 统一作业流程与总目标

**总目标**：Mochi 要成为更能干的教师助手、能覆盖老师日常工作的平台、老师真实的 AI 工作入口——对学校真实工作真正有帮助，而不是看起来很厉害。

```
理解材料 → 读取课表/成绩/学生记录 → 确定输出物 → 调用工具 → 检查异常 → 交付 → 继续修改
统一工作循环：规划 → 创建 → 检查 → 有限次数修正 → 验证 → 交付
```

任何 Skill 不得只给建议、给模板、给空话；必须真的生成文件、写进记录或进入任务状态机。**不能只有 `generate_ppt()` 或 `生成报告()`，生成后就不管质量。**每个 Skill 都要能回答七件事：**输入是什么、参考了哪些真实数据、步骤是什么、产物在哪、任务能不能追踪、完成后能不能继续改、出错后老师怎么确认**。

**Skill / Tool / 引擎 / 模型的分工**：Skill 是工作方法；Tool 是具体动作；业务和计算引擎维护严格规则；模型负责理解、组织与交流。工具返回事实、成果和错误，不能只返回一句"已经成功了"。

## 11. 能力现状对照总表（核心章，15 项）

状态图例：✅ 已实现并验收 · 🟡 已有基础/需扩展或验收 · ⬜ 尚未实现

| # | Skill | 状态 | 现有承载 | 缺口与下一步 |
|---|---|---|---|---|
| 1 | smart-scan 智能扫描 | ⬜ | 无 | 裁边/旋转/矫正/清晰度检查，按顺序合成可打印 PDF；需图像理解管线；第一批依赖项 |
| 2 | document-digitize 文档电子化 | ⬜ | 无 | 恢复文字/表格/公式/阅读顺序，生成可编辑文档并标记疑点；可复用 PP-StructureV3 做版面解析（**解析结果 ≠ 完整无误的可编辑重建**，重建与核对仍属我们的工作链） |
| 3 | exam-paper-digitizer 试卷电子化 | ⬜ | 无 | 恢复题号/选项/分值/配图/答案关系，生成学生版和教师版；**与 #2 共用版面/OCR 底座，严禁各做一套** |
| 4 | spreadsheet-work 电子表格处理 | 🟡 | mochi-grades（成绩表读取/导出基础） | 合并/去重/分班/匹配/查缺漏，保留原件和差异；复杂表头/多表合并/口径核对能力待验收；全量重算防公式未刷新 |
| 5 | grade-analysis 成绩分析 | 🟡 | mochi-grades | 核对统计口径、由程序计算；区分缺考/缺失/零分，同名学生不自动合并，更必须有版本和来源；生成可复核结果与教学分析（保留原始数据/口径/异常名单/生成文件链路） |
| 6 | teacher-document 教师文书 | 🟡 | mochi-documents | 教案/通知/班会记录/活动方案/周报/总结等真实 Word；soffice 依赖未清（第 24 章）；学校红头等模板沉淀 |
| 7 | lesson-presentation 课件制作 | 🟡 | mochi-presentations + class-meeting-prep | 真实生成 PPT、检查版式与内容、**继续修改指定页面**（改第三页不重新从头生成）；soffice 依赖未清 |
| 8 | batch-document-converter 批量转换 | ⬜ | 无（现依赖 soffice） | 图片/PDF/Word/PPT 按实际支持范围转换、合并、拆分；与 #6/#7 共用转换底座 |
| 9 | teacher-file-organizer 文件整理 | ⬜ | 无 | 在授权目录提出分类/改名/移动方案，**确认后执行并保留记录**；接偏好记忆 |
| 10 | teaching-material-find 资料查找 | ✅ | skills/teaching-material-find + mochi.find 共享登记 + mochi-web-search | 先找自己与公共授权资料，必要时向其他 Mochi 请求；按年级/学科/单元索引持续补料 |
| 11 | schedule-coordinator 日程与调课协调 | 🟡 | mochi.ask / mochi.request 传话链路 | 创建待办/准备提醒/协调会议或换课，明确执行和审批状态；读正式系统排课结果/写回属外部接口，**本期不做**（降级：冲突检查报告供人工提交）；完整排课见第 41 章（终态，求解器路线） |
| 12 | class-meeting-prep 班会准备 | ✅ | skills/class-meeting-prep | 目标/大纲/讲稿/PPT 全链路已在；补"根据班级问题 + 学校通知 + 近期表现"的数据联动验收 |
| 13 | question-bank 题库与试卷重排 | ⬜ | 无（依赖 #3） | 经核对后按题型或知识点整理、组卷、生成对应答案版本 |
| 14 | homework-review 作业分析与讲评 | ⬜ | 无 | 整理常见问题、匿名化例子和教学建议；**评分建议由老师复核** |
| 15 | workflow-combo 组合工作流 | 🟡 | 各单项能力的串联模式 | 成绩表→分析→Word 报告→会议 PPT；试卷照片→电子卷→交互模型→讲评课件；以任务链形式验收 |

> 口径说明：✅ 的"验收"指本仓已有技能/插件级验证记录；面向答辩的端到端 Golden Path 总验收统一列入 P5（第 45 章），标 ✅ 不代表无事可做。

## 12. 已有但清单未列的能力（保留，不重复建设）

| 能力 | 承载 | 说明 |
|---|---|---|
| 教师每日简报 | teacher-daily-brief | 今日课表/待办/异常一屏 |
| 学生跟进 | student-follow-up | 个体学生的流程跟进 |
| 学生外出/就医申请 | student-movement-request | 嘉行联业务联动 |
| 班级周报 | weekly-class-report | 周期性汇总产物 |
| 传话/任务/寻物 | mochi skill + mochi-dispatch | 六端传话网络，已通过可靠性验收 |

## 13. 共用地基要求（所有 Skill 实现前必须就位）

这些是**共用地基**，禁止每个 Skill 各做一套：

1. **任务关联**：调用工具/生成文件/等待确认，统一进 mochi-dispatch 任务状态机，可追踪。
2. **交付标准**：默认交文件路径 + 简短说明；不得只描述"我可以帮你做"。
3. **运行位置**：能本地就不上云（模型/文件/OCR 默认本地或校内）；需要用户先拍板的事，先说清楚再动手，不拖到最后。
4. **授权与确认**：读数据尽量直接读；外发/改写/批量处理必须先经老师确认（审批闸）。
5. **低置信对照模式**：拍照、手写、公式、表格转换不假装全对——结果页 + 原图对照页 + 待确认区，老师只核对不确定处。
6. **偏好记忆**：学校、年级、学科、常用格式、常用软件记住，下次直接用（接第 28 章偏好引擎与第八部分记忆系统）。
7. **测试纪律**：每个 Skill 必须回答七件事（第 10 章），并有异常场景测试（材料模糊/表格列错位/学生重名/成绩缺一次考试/生成的 PPT 第二次继续改）。
8. **多说法鲁棒**：老师换一种正常说法仍能理解（"我们班""刚才那个""按上次格式""第三页改一下""去问问那个老师"），不依赖固定短语——普通聊天必须真正调用模型，本地代码只做 authorize/deny/redact/require approval/constrain tools，**不生成最终自然语言回答**。

## 14. 推进顺序

**本期目标不是做最多 Skill，而是先把老师每天都会遇到的事做穿。**

- **第一批重点交付（三件事）**：
  1. **纸质资料 → 可编辑电子文档**（#1→#2，#3 跟进）
  2. **成绩表 → 可复核的分析结果**（#4→#5）
  3. **教案/材料 → 可继续修改的上课 PPT**（#7）
- **每完成一件事必须能回答三句话**：老师少了哪一步重复劳动？生成的结果老师能不能继续改？下次再做同类事是不是更快？
- **本期怎么验证有没有做对**：① 老师愿不愿意马上用？② 老师是不是少了手动操作？③ 结果能不能继续改？④ 下次再做是不是更快？⑤ 出问题能不能找到任务记录？
- **三个不准**：不准做面向管理者的花哨分析；不准做脱离真实工作流的演示功能；不准做学生数据相关的越界设计。
- **开工原则**：从现有 Mochi 项目继续，不推翻、不另起一套、不重来；先检查再继续；工程顺序 REUSE → EXTEND → LOCAL REFACTOR → CREATE（不重造 Harness/Sidebar/Browser/Tool Runtime/File Manager）；第一批只交付三件事，不做大杂烩。
- **第二梯队**：#6（与 #7 共用文档底座）、#8、#9、#13、#14。
- **第三梯队**：#11（受外部系统接口约束，保持传话协调形态）、#15 组合工作流、既有 5 项能力的打磨。
- **选型原则**：成熟开源/本地工具优先；必须有真实文件产物；必须接任务状态机；必须可被老师继续修改；必须支持二次编辑，禁止"演示一次就结束"。

---

# 第四部分 · 教学建模工作空间（新增核心能力）

> 来源：《最终能力与功能总纲》§二–§六、§十八、§二十。这是与办公、协作并列的第三条主线：**把抽象知识变成学生能理解的模型**。

## 15. 目标与五种成果形态

- **输入**：文字、公式、题目图片、已有教案或实验数据 → 课堂可用的模型。示例："做一个椭圆切线模型，可以拖动切点，显示切线、焦点和必要的辅助线，先隐藏结论"；"做一个平抛运动演示，能调整初速度和高度，能暂停，同时显示水平与竖直分运动"；"这份实验数据帮我画图、拟合，再把理论曲线和实测点放在一起比较"。
- **达标线**：**能运行、能操作、能解释、能保存、能修改、能分享，并且能核对。**

| 类型 | 解决的问题 | 交付形式 |
|---|---|---|
| 静态教学示意 | 看清结构、关系和标注 | 清晰图片、矢量图、讲义配图 |
| 交互数学模型 | 观察参数、点、线、面变化时的关系 | 可拖动的几何或函数页面 |
| 物理过程仿真 | 观察运动、受力、能量、光线等变化 | 有播放与参数控制的仿真页面 |
| 数据建模与分析 | 比较数据、拟合关系、分析误差 | 图表、计算结果、模型说明 |
| 分步讲解动画 | 按教学顺序展示构造、推导和变化 | 分步演示或视频 |

- **两条验收红线**：不能把"生成了一段视频"标成"已经支持交互建模"；不能把"生成了一张三维图片"标成"已经支持三维模型"。

## 16. 数学建模清单与特别要求

| 功能 | 学生可以怎样操作和观察 | 老师可以怎样使用 |
|---|---|---|
| 函数与参数变化 | 调整参数，观察图像平移、伸缩、交点和定义域变化 | 对比多组参数，保存典型状态 |
| 导数与切线 | 拖动曲线上点，观察切线、割线与斜率变化 | 分步展示概念形成，按需显示公式 |
| 圆锥曲线 | 改变椭圆/双曲线/抛物线参数，拖动点，观察焦点、准线、切线及轨迹 | 将具体题目转换为可操作模型 |
| 向量与三角关系 | 拖动向量，观察合成、分解、投影、夹角与对应数值 | 同步展示几何关系和代数表达 |
| 平面几何构造 | 拖动条件点，观察垂直、平行、相似、圆与轨迹关系 | 逐步添加辅助线，隐藏或展示结论 |
| 立体几何 | 旋转视角，观察点线面关系、投影、截面与展开 | 切换视角、强调遮挡关系、分步说明 |
| 概率与统计实验 | 调整样本量或试验次数，比较频率与理论值 | 展示随机波动，导出实验记录 |
| 数据拟合与简单优化 | 比较拟合方式、残差和参数变化 | 连接实验数据或现实问题，说明模型假设 |

**数学教学特别要求**：

- **观察不是证明**——拖动很多次都成立，只能支持猜想，不能自动宣布完成证明。
- 系统应允许老师切换：**观察模式 → 提问模式 → 辅助线模式 → 分步推导模式 → 结论模式**。
- 涉及推导时必须说明使用条件；不能为了让图形"看起来成立"，悄悄改变题目中的点、线或参数。
- 超出当前模板和验证能力的构造，进入"自定义模型草稿"，明确需要核对，而不是无条件保证正确。

## 17. 物理建模清单与特别要求

| 功能 | 可调整条件 | 同步展示 |
|---|---|---|
| 匀变速与平抛、斜抛 | 初位置、初速度、角度、重力等已定义参数 | 轨迹、位移、速度、加速度、分运动图像 |
| 斜面受力与摩擦 | 倾角、质量、摩擦条件、外力 | 受力图、分力、状态判定与运动变化 |
| 弹簧与振动 | 质量、劲度系数、初始条件，扩展时加入阻尼 | 位移、速度、能量及相位关系 |
| 碰撞与动量 | 质量、初速度、选定碰撞模型 | 碰撞前后速度、动量与相应能量变化 |
| 圆周运动与约束 | 半径、速度及已定义的约束条件 | 速度方向、加速度方向、受力关系 |
| 几何光学 | 物距、焦距、介质或界面条件 | 光路、像的位置与性质，适用范围说明 |
| 基础电路虚拟实验 | 理想电源、电阻、串并联与测量位置 | 电流、电压、功率及电路图 |
| 波动与干涉 | 波长、频率、相位等参数 | 波形、叠加过程、空间与时间变化 |
| 电场、磁场与电磁感应 | 经验证模板中的电荷、场和运动条件 | 场的表达、受力或感应量变化 |

（**最终覆盖目录，不要求第一版全部实现**。）

**物理教学特别要求**：

- 每个模型必须明确：**研究对象、坐标系、单位、初始条件、使用规律、忽略因素、适用范围**。"忽略空气阻力""理想弹簧""小角度近似"等条件不能藏在代码里不告诉老师。
- 仿真数据必须标为仿真数据，不能包装成真实实验测量。
- 守恒检查必须符合模型：有摩擦、阻尼或外界做功时，不能错误要求机械能始终不变。

## 18. 八步交付流程（模型从提出到交付）

1. **读取需求和原材料**：文字/题目/公式/图表/实验数据；优先读取原有电子内容，只有无法直接读取的扫描材料才做图像识别；题图不清、条件缺失或符号歧义时**先标记问题，不自动补造条件**。
2. **先定"学生应该理解什么"**：先明确教学目标（"帮助学生区分速度方向与加速度方向"），而不是立即生成一堆运动元素。
3. **形成模型说明**：对象、变量、参数范围、公式或约束、图形、交互方式、讲解步骤和预期观察；明确区分**老师提供的条件、系统采用的假设、仍需确认的问题**。
4. **优先组合已验证的模板**：能参数化配置就不从零生成整段程序；支持在模板上加辅助线、改展示方式、组合图表；确需自定义代码时作为**受控扩展**执行，不能直接在拥有学校账号或文件权限的主界面运行任意代码。
5. **计算和画面使用同一份模型状态**：位置、数值、曲线、公式必须一致——不能动画是一套计算、图表是另一套、解释来自第三套猜测；播放快慢与物理时间、数值步长正确区分。
6. **验证**：参数边界、单位、定义域、退化情况、已知参考值和数值误差；有解析参考时对照，采用数值方法时记录方法与容差；计算库可以辅助核验，但**"程序没报错"不等于理论正确**。
7. **教学检查**：请老师确认模型是否对应教学目标、哪些操作有帮助、哪些元素干扰理解、是否需要先隐藏结论。**模型正确和模型好教，是两项不同验收。**
8. **交付并支持继续修改**："保留模型，把辅助线先隐藏""给学生留一个预测问题""把运动放慢，分两步讲""把这组参数保存成第二个对比场景"——修改后保存版本，**不覆盖已确认版本而不留记录**。

## 19. 界面三模式与成果管理

- **制作模式**：老师表达需求、修改参数、检查假设与结果。**课堂模式**：突出模型画布，支持全屏、大字号、暂停、单步、重置和分步揭示（落到教室端大屏，第 27 章）。**学生探究模式**：使用老师发布的版本，在允许范围内操作，记录预测、观察和回答。
- **必备交互**：拖动点或对象、参数滑块、直接输入数值、播放与暂停、单步、重置、条件对比、辅助线开关、公式开关、视角切换、观察记录；键盘和触摸都可用；不能仅靠颜色或动画传达信息。
- **模型是正式成果**：与 PPT、Word 一样进入统一成果管理（第 43 章），记录模型标识、版本、参数、公式与假设、运行依赖、来源、验证结果、审核状态、分享权限。
- **交付与导出说明**：交互页面保留操作能力；视频保留演示过程但不保留任意参数操作；PDF 和普通图片是静态成果；**放进 PPT 的截图或视频不自动变成可交互模型**——需要配套模型入口或独立运行包。
- **成本与性能原则**：**拖动一次参数不应再调用一次大模型**——参数变化由已生成的计算与渲染逻辑处理；目标是让已制作好的模型在测试过的课堂环境中使用，尽量减少上课时对模型 API 和网络的依赖。

## 20. 开源基础与首批小样

| 基础项目 | 适合承担什么 | 不能误认为它自动解决什么 |
|---|---|---|
| **JSXGraph**（LGPL/MIT 双许可） | 浏览器中的交互几何、函数、曲线及可视化；**建议先用于二维教学模型验证** | 不自动理解全部试题，也不自动证明结论 |
| **SymPy** | 代数、微积分、方程、几何及部分物理计算辅助 | 不能替代所有推导审核，也不是通用证明保证 |
| **Three.js** | 三维场景、立体几何和视角操作 | 是渲染基础，不自动提供正确物理规律；教室设备图形支持需实测，三维不作默认形式 |
| **Manim Community** | 分步数学讲解、程序化动画与视频 | 不能把视频输出直接当成浏览器交互模型；交互画布走独立的交互实现 |
| **OR-Tools** | 排课、排班等约束求解候选（第 41 章） | 不会自动知道学校规则 |
| **PP-StructureV3** | 扫描题目、公式、表格与版面解析（第 11 章 #2/#3） | 不保证无误重建任意试卷 |

- **接入纪律**：固定版本、检查代码及模型权重许可、记录依赖和运行资源；优先复用现有能力；**不同时引入多套做同一件事的引擎**。
- **首批建议三项（以两位老师实际准备讲的内容为准）**：
  1. **数学：圆锥曲线与切线**——验证拖动、辅助线、条件显示、分步讲解和模型保存；
  2. **物理：平抛运动与分运动**——验证运动、图表和数值同步，以及暂停、单步和参数变化；
  3. **物理：斜面受力与摩擦**——验证状态条件、受力方向和参数边界，不只展示一个滑块一直往下滑。
- **小样方法**：两位老师各提出一个真实教学问题 → 先做一个**能运行、能拖动、能核对、能保存**的最小模型 → 教师确认"准确、能帮助讲解、课堂设备可用"后才扩第二项。**不要先做十个漂亮的空壳页面。**

---

# 第五部分 · 桌面端原生化：双击即用

> 来源：《Mochi-桌面端原生化方案》（23 章完整版）。此处保留核心思路与关键细节；安装向导逐字文案、UAT 10 场景详表、自检 11 项明细以原文件为准（附录 A）。

## 21. 诊断结论与四个阻断项

**老师视角目标**：双击图标 → 直接可用。全程不出现浏览器、不出现终端、不需要任何配置。

| # | 阻断项 | 位置 | 后果 |
|---|---|---|---|
| 1 | 打包白名单只有 9 个插件，`runtime-profile.json` 已声明 12 个 | `scripts/prepare-mochi-resources.cjs` + `test-package-resources.mjs:191` 硬断言 ===9 | **打包版 mochi-web profile 起不来**（缺 mochi-web-search / mochi-llm-mimo / dsh-better-sidebar）。**✅ 已解决（2026-09-12 复核）：白名单现为 26 个插件，本行数字为 2026-09-06 的历史快照** |
| 2 | 打包 profile 资源目录疑似为空 | `.mochi-package-resources-v1.nosync/profile` 空 + `profile.ts:37` 要求 `runtime-profile.cjs` 存在 | 打包版**可能启动失败**（P0 第一件事验证） |
| 3 | 无品牌图标（无 .icns/.ico） | `apps/desktop/` | 默认 Electron 图标，不像正经应用（第 26 章规格已备，专人绘制） |
| 4 | `web-host.ts:94` `cwd: process.cwd()` | Finder 双击时 cwd=`/` | dsh 在根目录建文件 → 改 `app.getPath('userData')` |

**两个不用操心的事**：① 认证——桌面端拿带 `?token=` 的地址天然免登录；② symlink——staging 用 `cpSync(dereference:true)` 全解引用 + `assertNoSymlinks` 强校验。

**体验坑**：窗口 `show:false` + ready-to-show 才显示 → 双击后几十秒"没反应"。改法：**窗口立刻出现 + 品牌启动页与进度，就绪后原地换页**；失败给中文诊断页（一键复制日志、可重试）。

## 22. 安装 / 首启 / 自检 / 医生

- **macOS 分发主渠道：U 盘 / 校内共享盘**（隔离属性 quarantine 只跟着浏览器/QQ/微信下载走，U 盘拷贝不带 → 双击即开）。微信分发附"右键→打开"图文说明。`.command` 脚本自身也会被隔离，"用脚本修隔离"会自举失败，不做。
- **Windows**：沿用 NSIS 中文向导（一键安装 + 开机自启选项）。
- **首启向导**（仅第一次）：欢迎 → 模型密钥确认（内置默认可跳过，设置页可换）→ 搜索源确认 → 完成。可随时跳过。
- **环境自检 11 项 + Mochi 医生**：一键诊断报告（脱敏），老师能直接转发给技术同事。
- **冷启动**：先计时拿基线再定目标（参考值：M 系 Mac 秒级~15s、低配一体机 30-60s，配开机自启常驻化解）；启动页显示真实进度。

## 23. 启动体验 / 失败矩阵 / 日志隐私

- 启动时序：窗口即出（品牌启动页）→ sidecar 就绪 → 原地换页；90s 超时 → 诊断页。
- 失败矩阵覆盖：端口被占、密钥失效、搜索源不可达、校园服务不可达、sidecar 崩溃（自动重启 + 健康检查）——每条给中文说明 + 一个可点动作。
- 日志脱敏：密钥/token/学生姓名不入日志；诊断报告二次脱敏。

## 24. 零本机依赖：四项实现级硬改（不是改配置）

| 现在依赖 | 换成 |
|---|---|
| LibreOffice `soffice`（写死 `/opt/homebrew/bin/`） | 纯 JS：docx 库 + pptxgenjs 生成，pdf-lib 直出 PDF，不再"先生成再转换" |
| Docker + OnlyOffice（本地 18080） | 纯 JS 预览（mammoth / SheetJS） |
| 外部 `git` 命令（侧栏 Git 页） | isomorphic-git（纯 JS） |
| Hiragino Sans GB（Windows 没有） | 内置思源黑体，运行时注册 |

另有两处注入缺口：Electron 侧 `buildEnv()` 未注入 `MOCHI_CAMPUS_API_URL` / `MOCHI_SEARXNG_ENDPOINT`（mochi.sh 里有）——不补则校园功能兜到 `127.0.0.1:8787` 直接失效。**验收判据**：全新机器（无 Node/Python/Docker/LibreOffice/git、无源码目录、无 `MOCHI_*` 环境变量），只靠 U 盘安装包 + 校园网跑通全部功能。

## 25. 打包签名与分发矩阵

- **macOS arm64**（老师端主力，开发机同架构）：本机直接打，免签名 ad-hoc + U 盘渠道。
- **macOS x64 / Windows x64**（教室端主力）：GitHub Actions 或借实体机（仓库当前无 remote、50 个未提交改动 → **先建基线提交**）。
- **系统底线**：Electron 39.8.10 要求 macOS ≥ 12、Windows ≥ 10；老一体机若是 Win7 需降级 Electron（代价大，先摸机型）。
- **开机自启**：`app.setLoginItemSettings()` 未签名会静默失败；教室端常驻改为安装时配计划任务/启动文件夹（Windows）或 LaunchAgent（macOS）。
- **单实例锁 + 托盘常驻**：防老师双击两次起两个实例。

## 26. 图标与视觉资产

- **只缺两件**：App 图标（.icns/.ico）+ 托盘图标。界面内 Mochi 形象复用 `OrbCompanion` 组件代码（球 + 面前一台小电脑），**不重画**。视觉总原则见第 5 章。
- **专人绘制**，交付规格已定（可直接转交画师）：球 `#E8B34B`、描边 `#B98A5C`、高光 `#FFF6E8`、电脑 `#6B4E33`/`#4A3826`、五官 `#2E2318`、深绿底 `#45584e`；球居中偏上、电脑在球前下方宽约球径 78%；**禁止纯球体、禁止用水母**；交付 SVG + iconset 十张 + .ico + 托盘模板图；≤32px 去屏幕字符、≤16px 只留色块。
- 图标不挡进度：先用现有校园 `icon.svg` 占位，正式图标到了只换文件重打包。

---

# 第六部分 · 教室端能力

## 27. 场景清单

| 场景 | 流程 | 承载 |
|---|---|---|
| **打开软件/文件投屏** | 教师端经 A2A 向教室端发 REQUEST（"把这个给学生看" + 文件）→ 教室端收下后按自己的规则执行：调系统默认程序打开（docx→WPS，课件→希沃），投上大屏 | A2A over LAN（第七部分）+ 打开软件能力 |
| **课前预启动** | 按课表 + 该老师的软件偏好，课前 N 分钟自动开好要用的软件（物理老师→希沃提前打开，英语老师→C30）。教室端本机独立完成，不需要教师端在线 | 偏好引擎 + 课表数据 + 打开软件能力 |
| **学生问答** | 学生在大屏前直接对教室 Mochi 提问、发消息（对话层，无需任何账号），Mochi 完整作答（知识不受限，第 3 章）；想找老师 → 留言由教室 Mochi 经 A2A 转给教师端 | 学生角色工具面收敛 + A2A 留言 |
| **大屏通知点名** | 教师端经 A2A 发通知请求（"喊 XX 到我办公室"）→ 教室端大屏弹出醒目横幅 → 学生看到 | A2A 通知消息 + 大屏通知组件 |
| **教学模型课堂模式** | 教师端制作/审定模型（第四部分）→ 经成果共享或 LAN 发到教室端 → 课堂模式全屏展示（暂停/单步/分步揭示/触摸拖动） | 教学建模（第四部分）+ 大屏课堂模式 |

**关于"打开并登录"**：打开软件 = 一条系统命令（Windows `start` / ShellExecute），可靠简单，**不是**模拟点击的 GUI 自动化。登录态预期靠软件自身记住（**需真机验证**：希沃/WPS 一般能保持，C30 待确认；不能保持则首次人工登录，之后 Mochi 只负责"到点开好"）；真·自动登录（UI 自动化填密码）列为可选增强，不作本期承诺。

## 28. 个性化与课表联动（偏好引擎）

- 数据结构：`teacher → subject → apps[]`（例：王老师/物理 → [希沃, WPS]；李老师/英语 → [C30, WPS]），存本地 `~/.mochi-home/classroom/`。
- 录入方式：对话里说一次即记住（"我上物理课用希沃"），也可设置页手改；软件别名可配（"希沃"→`EasiNote.exe`，常见路径内置候选表）。
- 预启动触发：课表时间 - N 分钟（N 可配，默认 5）；**课表数据来源（mochi-campus 教师课表 / 手动录入 / 正式系统导出）待确认**（附录 B）；无课表数据时降级为老师一句话触发（"准备下节课"）。
- 偏好引擎同时服务教师端（常用格式/模板/学科）——最小版先用本地 JSON 落地，记忆系统（第八部分）就位后迁移复用，不另起炉灶。

## 29. 交互形态

- **语音输入是刚需**：老师站着讲课不能打字。联动计划侧已有语音模式实践，**Mochi 侧尚未接入**（列入 P3）；教室端默认开启。
- **大屏 UI**：字号/对比度按 3 米外可读重做，点击热区按触摸屏放大；通知横幅全屏顶部弹出 + 自动消隐。
- **学生侧界面**：问答对话框为主，无设置入口、无任务入口（功能受限的界面体现）。

## 30. 设备约束与对策

| 约束 | 对策 |
|---|---|
| 低配一体机（Celeron/N100 + 4~8GB） | 开机自启常驻，课前零等待；冷启动 30-60s 只在开机时付一次；教学模型二维优先，三维需实测 |
| 还原卡/管控（重启即抹） | 应用绿色免安装 + 数据外置 U 盘/学校 IT 加白名单；身份与偏好（含相识列表）支持"导出配置包"，30 秒恢复 |
| 系统版本（可能 Win7/10） | Electron 39 需 Win10+ → **先摸清教室机型**（附录 B 待确认） |
| 教室网段受限 | LAN 直连不依赖外网（第七部分）；模型 API 需外网 → 答辩前必须教室网实测 |
| 无麦克风机型 | 降级触摸+软键盘；语音列为增强 |

---

# 第七部分 · 局域网设备直连通信方案

## 31. 目标与现状

- **现状**：mochi-dispatch 的跨端投递走 campus relay（服务器中转一句话）。服务器不可达 = 跨端能力全断；且 relay 只能递一句话，**传不了文件**。
- **目标**：给 **A2A 加一条 LAN 直连通道**——同一校园网内的 Mochi 之间不经过任何服务器，直接完成消息、文件、通知的协作投递；relay 保留为跨网段/外网（老师在家）的 fallback。
- **前提认知（用户 2026-09-06 裁定）**：教室端是**独立 Agent**（第 2 章），两端沟通本来就是 A2A——LAN 直连只是给 A2A 换一条不依赖服务器的传输通道，**A2A 权限模型一层不变**。
- **为什么可行**：教师端与教室端在同一栋楼、同一有线网/VLAN 的概率极高；两端跑的是同一套 Mochi（不同 profile）；Node 内置 `dgram/http/crypto`，**零新依赖**，符合零本机依赖硬约束。

## 32. 总体设计（四层）

```
┌─ 融合层 ── mochi-dispatch：transport=lan，7 态机与 A2A 权限模型不变，LAN 优先 relay 兜底
├─ 传输层 ── peer HTTP 服务（每实例内置，TCP 47832）：message / file / ack 三类端点
├─ 相识层 ── 首次人工确认（像加好友）：交换 endpointId + 显示名，之后互认
└─ 发现层 ── UDP 广播信标（主）/ 组播（备）/ mDNS（增强）/ 手动 IP（兜底）
```

## 33. 设备发现（Discovery）

- **主方案 · UDP 广播信标**：每个实例每 5s 向 `255.255.255.255:47832` 发 JSON 信标 `{proto:"mochi-lan/1", endpointId, name, role, port, ts}`；所有实例监听同端口，维护在线设备表（TTL 15s，超时下线）。`role` 区分 teacher / classroom / generic。
- **备选 · 组播**：`239.86.79.67:47832`，用于交换机禁广播的网络。
- **增强 · mDNS**：`_mochi._tcp.local`（macOS 原生 dns-sd；Windows 需 Bonjour 服务，**不作为依赖**）。
- **兜底 · 手动 IP**：设置页添加 `IP:端口`，保存即长期有效。**手动 IP 是一等公民不是隐藏功能**——跨 VLAN 只要路由可达，TCP 直连就能通（广播才需要同广播域）。
- 信标不携带任何敏感信息（仅 id/name/role/port）；设备显示名必须老师可改（默认机器名会泄露 `DESKTOP-XXXX`）；信标只发 IPv4（避免 IPv6 链路本地地址的 zone id 问题）。

## 34. 相识与治理（安全模型）

**核心认知（用户 2026-09-06 裁定）**：这套系统的安全不靠密码学堆叠，靠两层本来就存在的东西——

1. **数据隔离是天然防线**：教室端不登录老师账号，它本地根本没有老师的私密数据。学生在大屏前能触达的，只有教室端自己的东西；教室端能做的事，也只有它自己工具清单里的事（功能受限，第 3 章）。
2. **A2A 权限模型是主防线**（第 8 章，已实现）：任何跨端请求必须显式发送；接收方自己决定答不答应；任何 Agent 不得私自代表另一个人答应。**发现越界的 Agent，就不允许它工作**——教师端可以拒收某设备的消息、解除相识、把它从协作网络里拿掉。

**相识流程（首次人工确认，像加好友）**：

1. 教师端在设备列表里看到"高二(3)班教室"（发现层提供）→ 点"认识一下"；
2. 教室端大屏弹确认："王老师的办公机想与你协作，同意？"→ 在场的老师/管理员确认；
3. 双方互存 endpointId + 显示名，之后互认，无需再确认。

**治理动作**（都是一句话/一次点击的事）：**拒收**（不理会某台设备的消息）、**解除相识**（回到陌生人）、**禁用**（把越界 Agent 从协作网络里拿掉）。学生恶作剧留言不是安全问题，是管理问题——留言天然署名来源（哪个教室哪台设备），教师端可拒收、可拉黑。

**可选加固（二期，非必需）**：TLS 自签 + 指纹钉扎、消息 HMAC 签名。一期校园内网默认不做——网络是学校自己的，治理靠上面的模型已经足够。

## 35. 消息与文件传输协议（Transport）

- 每个实例内置 peer HTTP 服务（`node:http`，零新依赖），监听 **TCP 47832**（可配置），只响应**已相识**设备的请求（按 endpointId 白名单，陌生人直接拒）。
- 端点：
  - `POST /mochi-lan/v1/message` —— ≤64KB 的 JSON 消息（A2A 请求/留言/通知）；
  - `POST /mochi-lan/v1/file` —— 文件传输（元数据头 + 分块，单文件默认上限 100MB 可配，SHA-256 校验）；
  - `POST /mochi-lan/v1/ack` —— 投递确认。
- **确认语义（比 relay 更强）**：接收方**落库后**才返回 200 + ackId → 发送方据此把任务置 DELIVERED（确定投递）；连接失败/超时 = 明确 FAILED，可安全重试。**直连天然消除 relay 的"未知投递结果"状态**，状态机少一个坑。
- 收到的文件落 `~/.mochi-home/lan/inbox/`（配额 + 扩展名白名单 + 定期清理），**不自动执行**；打开动作必须走审批闸。

## 36. 与 mochi-dispatch 的融合

- `mochi_tasks.transport` 新增 `'lan'`。投递路由：对端 LAN 在线 → 走 lan；不在线但 relay 可达 → 走 relay；都不通 → 明确失败（不静默）。
- **7 态机与 A2A 权限模型完全不变**（CREATED → DISPATCHING → DELIVERED → …；"谁有权请求、谁决定答应"的规则原样沿用），只是投递函数多一个通道实现。
- 新能力：**附件**。先发文件得 `fileId`，消息体携带引用——教师端"把这个文件给学生看"才有载体。
- 通知类消息（NOTIFY：点名/提醒）→ 教室端收到后由大屏通知组件弹横幅（第 27 章）。
- 与 relay 并存时的去重沿用现有 `idempotency_key` 机制。
- **UI 诚实**：跨端不可达时界面明确显示"离线模式：跨端功能暂不可用"，不假装已发送。

## 37. 可行性论证与注意事项

**可行性**：

1. 两端同一 codebase，peer 服务是**插件级新增，不动 dsh 内核**；
2. 零新 npm 依赖（dgram/http/crypto 全为 Node 内置）；
3. 教师办公室与教室同楼同网段概率高；即使跨 VLAN，手动 IP 兜底仍可不依赖服务器；
4. 信标流量可忽略（5s × N 台 × 百余字节）。

**注意事项（实现者必读）**：

1. **不要复用 dsh web 的 3090 端口与 token 体系**做 peer 通信——那是浏览器向 401 认证；peer 独立端口 + 独立相识体系，职责分离。
2. Windows 防火墙首次会弹"允许网络访问"→ 写进安装向导一次性放行（或安装时预配规则）。
3. 广播/组播都可能被学校交换机禁用 → 手动 IP 必须可靠且好找。
4. 还原卡会抹掉 `~/.mochi-home` 里的身份与相识列表 → 提供"导出/导入配置包"，重新相识 30 秒完成。
5. 相识列表在设置页可管理：改显示名、解除相识、拉黑，都是一次点击。
6. 文件接收目录设配额并定期清理；只接受白名单扩展名（docx/xlsx/pptx/pdf/图片/音频）。
7. 同一教室可能有多台设备（大屏 + 实物展台），role 字段区分，通知只投大屏。
8. 端口冲突处理：47832 被占则自动顺延并更新信标。
9. 测试必须覆盖：大文件断点 / 断线重连 / 还原卡重置后重新相识 / 陌生人消息被拒 / 解除相识后请求被拒。
10. 信标里不放任何敏感信息（仅 id/name/role/port），避免被扫描画像。

---

# 第八部分 · 记忆系统

> 来源：《记忆系统调研_2026-09-06》+《最终能力与功能总纲》§十一。此处保留落地设计；完整调研以原文件为准。

## 38. 五层记忆与边界

| 记忆类型 | 保存内容 | 边界 |
|---|---|---|
| 当前对话 | 当前讨论、指代与上下文（"刚才那个""继续""第二个""按刚刚那个方案"） | 不跨用户混用 |
| 当前任务 | 目标、已确认要求、正在处理的成果（"正在准备周五家长会 PPT"） | 随任务归档或到期自动衰减 |
| 个人偏好 | PPT 风格、报告习惯、常用格式、常用班级、常用工具 | 用户可查看、修改、删除、关闭记忆 |
| 历史工作 | 做过什么、使用哪个模板、对应哪个成果 | 关联实际版本，不凭空回忆 |
| 组织知识 | 制度、作息、岗位关系、公开模板 | 有来源、生效时间和访问范围 |

- **写入纪律（Memory Confidence）**：长期记忆只在"明确表达偏好 / 多次稳定重复 / 用户主动要求记住"时写入——不能因为用户说过一次"我喜欢蓝色"就永久写入。
- **敏感边界**：成绩明细、医疗内容、密码、令牌和私人文件全文，不自动进入长期偏好记忆；"我是某班班主任"以认证关系为准，不能仅凭聊天写入就获得权限。
- **可检查可纠正**：界面支持"为什么 Mochi 知道这个"（展开看到来源：用户偏好/上次任务/当前对话）、"这条不对"、"忘记它"、"关闭记忆"；删除时说明作用范围——删除个人偏好与按制度保留的业务记录不能混为一谈；新增长期记忆不频繁弹窗（低风险偏好后台建候选，明确长期信息自然提示"我记住这个格式偏好了"）。

## 39. 选型结论

- **首选范式：SimpleMem 式衰减**（纯 stdlib sqlite3 可实现）：`decay_after_days=30 / decay_factor=0.05 / min_importance=0.15`，线性/指数两种衰减，`supersede(old,new)` 取代链 + `reinforced` 强化计数。被召回时强度 +1 且计时归零（MemoryBank `R=e^(-t/S)` 同款机制）。
- **冲突消解：双时态模型**（Zep 范式）——`valid_at/invalid_at` 与 `created_at/expired_at` 分离，矛盾时旧边置 `invalid_at` 不删除。
- **"不该忘"免疫区**（MIRIX Knowledge Vault 范式）——pinned 记忆不参与衰减。
- **最重要警告（A-MEM 消融实证）**：无链接无演化的碎检索记忆 F1 **9.65**，低于不做记忆的基线 25.02——**做糙了会拖垮模型**。必须配引用率护栏：连续 20 次引用率 <20% 则收紧 top-k 8→5。
- **MemGPT 没有遗忘机制**（逐字核实），只做换入换出；mem0 OSS 无衰减；graphiti 后端已 archived → 均出局。
- **先不上向量检索**：SQLite FTS5 足够，零成本零依赖；需要时再上 sqlite-vec。

## 40. 表结构与两步走

- **第一步（1-2 天）· world-state.md**：纯文件，AI 每次读（≤200 行：待办/近 24h 变更/已知约定）+ `events/*.jsonl` append-only 原始事件。
- **第二步（3-5 天）· SQLite 记忆层**：
  - `mochi_memories`（pinned 免疫 / strength R / recall_count S / valid_at+invalid_at 双时态 / superseded_by 取代链 / source_task_ids 证据引用）；
  - `mochi_memory_events`（append-only 审计）；
  - `mochi_memory_injections`（引用率护栏数据源）；
  - FTS5 虚表做检索。
- 偏好引擎（第 28 章）最小版先用本地 JSON 落地（不阻塞 P2/P3），本层就位后迁移复用——不单建第二套。
- 记忆作用域 `scope_id`（按班级/按教师/全局）**待用户拍板**（附录 B）。

---

# 第九部分 · 岗位、学生端与成果共享（终态视野）

> 来源：《最终能力与功能总纲》§八、§九、§十四、§十五。本章是**终态视野**：除特别标注外，本期不实现，但架构不得写死阻碍这些扩展。

## 41. 岗位工作空间与嘉行联原有业务

- **教务处与教学管理**（终态覆盖）：课表导入、冲突检查、局部调课、代课协调、完整排课、考试时段、考场与监考、成绩汇总与更正、发布管理、教学资料和课时台账。**排课必须分开**：模型理解要求 → 人员确认约束 → 求解器计算 → 校验 → 预览差异 → 授权发布；不能让大模型直接生成一张课表就宣称"最优"——求解结果必须区分"找到可行方案 / 证明无解 / 尚未在限制内求出"。
- **德育处与学生事务**：学生流动和归寝汇总、异常事项督办、班级工作汇总、活动组织、值班协调、待跟进事项、事实时间线和归档。AI 整理事实与提出跟进建议，但不自动认定违纪、不生成"人格风险评分"、不擅自作纪律处分决定。
- **后勤与信息技术岗位**：报修、接单、转派、维修反馈、验收、设备借还、场地保障、库存查询、物资领用申请、巡检维护和台账分析。明确区分**已接单、已处理、待验收、已关闭**；采购和正式支出只能按学校既定审批办理，不能由 Agent 自动承诺。
- **医务室与宿舍**：继续保留到达、离开、留观状态、授权医务摘要、宿舍登记、迟到、无申请到达、异常处理和返班协作；医务正文保持专业，不通过角色动画表达诊断或处置结论。
- **组织与系统管理**：账号激活、人员与岗位、班级关系、能力配置、插件连接、审批规则、通知渠道、审计、运行状态和数据维护；**系统管理员不默认拥有读取全部业务隐私的权限**。岗位映射是产品配置，最终由学校实际分工决定，不写死某个部门必然负责某类事项。
- **嘉行联原有业务保留，不被新功能覆盖**：学生流动继续围绕教室→宿舍、教室→医务室及返班；保留教师放行、目的地到达和离开登记、教师返班确认、到达超时、返班超时、取消、异常记录、消息与统计；预计时长允许按情况调整，原因尽量用选项减轻录入负担；出行异常继续按已确定的职责由出发教师和班主任跟进，**增加部门端不能悄悄改变责任关系**；通知渠道"是否接受、是否送达、是否阅读、是否人工知悉"分别记录，没有可靠阅读证据就不显示"已读"；原有登录、今日、流动、消息、医务、学生、统计、设置及异常页面继续保留并验收。

## 42. 学生端最终能力（终态，本期不做）

- **学生端不是教师端隐藏几个按钮**。最终覆盖：个人学习资料、课程与任务、老师发布的交互模型、错题和学习笔记、知识点讨论、学习计划、授权账号连接、资料请求和校园申请。
- **教学模型的学生侧用法**：**先预测 → 操作模型 → 记录观察 → 解释原因 → 查看老师允许的提示**。老师可以发布某个模型版本、限定参数范围、设置问题、选择何时显示结论；学生可在授权下提交自己的模型副本或实验记录，老师看到的是明确共享的结果，**不默认查看学生全部私人聊天**。
- 接入外部学习账户时，使用对方支持的连接与认证能力；不能因为叫 MCP 就假定可以连接任何账号。
- **最终成绩、学习能力判断和重要教育决定由教师负责**；系统不生成固定的人格标签，也不把使用时长当成学习效果本身。
- 本期约束（任务书 PHASE 20）：学生端只保证 Domain / Runtime / Memory / Dispatch 未来能复用，不大写学生 UI；等教师核心稳定后再开发 Student Mochi。

## 43. 成果、知识库与共享

- **所有输出统一进入成果管理**：PPT、Word、Excel、PDF、图片、视频、交互模型、计算结果、课表方案和任务记录。每项成果有来源、拥有者、版本、权限、相关任务、检查结果和可用操作。
- **操作**：搜索、预览、继续修改、版本比较、复制为新任务、授权分享、撤销后续访问和归档。
- **知识库与资源库**：学校制度、教师模板、公开教学资料、审核过的模型、教学案例、个人授权文件。检索结果应能回到来源；不能把过时制度或其他学校材料直接当成本校当前规则。
- **诚实边界**："已经撤销分享"不能承诺删除别人此前已合法下载的文件副本，界面应说明实际作用范围。

---

# 第十部分 · 下一阶段工作计划

## 44. 任务书 PHASE 0–20 现状对照与工作边界

> 任务书另有优先级 P0–P4（迁移/A2A/记忆 → Dispatch/Artifact → 工具/UX → 桥接 → 学生端），与本方案第 45 章的实施波次 P0–P5 **不是同一套编号**。
> **给实现者的工作边界（任务书 §二十二原话）**：先把当前仓库映射到能力目录，逐项标记**已验收 / 部分实现 / 仅界面或文档 / 未实现 / 外部条件受限**，不要立即把所有功能同时开发；复用现有成果管理、工作区、权限和任务系统。

| PHASE | 内容 | 现状 | 本方案落点 |
|---|---|---|---|
| 0 | 迁移真实性与工程验收 | 🟡 **PARTIAL**（⚠️ 2026-09-12 更正：被引报告自身结论为 PARTIAL —— `MIGRATION_AUDIT_REPORT.md:7,11` 写「仍未满足 Phase 0」） | — |
| 1 | A2A 真实有效性验收 | 🟡 **PARTIAL**（⚠️ 2026-09-12 更正：`A2A_RELIABILITY_REPORT.md:3` 写「不能作为 Phase 1 已通过的证明」） | — |
| 2 | Relay → Dispatch v1 | ✅ 已完成（四原语 + 7 态机） | 第 8 章 |
| 3 | Artifact Exchange | 🟡 FIND+共享登记有雏形；统一 Artifact Contract/Grant 未做 | 第 36/43 章 |
| 4 | Agent Discovery | ⬜ Capability Registry 未做（现靠显式指定 peer）；方向=结构化 capability + LLM 选择 | 第 8 章设计，本期靠名录（9.5 章导入） |
| 5 | Memory v1（五层） | 🟡 调研与表结构完成、未实现；五层模型已对齐；Confidence/forget/inspect 未做 | 第 38–40 章、P5 |
| 6 | 自然语言去规则化 | 🟡 dsh 本体模型驱动；残留 canned response 待清；目标 Local Rule Direct Response Rate < 5% | 第 13.8 条、持续项 |
| 7 | Chat UX | 🟡 Streaming/取消/重试 dsh 自带；引用 Artifact/页面/Memory、"查看过程"收敛未做 | 第 7 章、P1/P3 |
| 8 | Work UX（首页极简） | 🟡 **部分完成**（2026-09-12 更正：`mochi-modes` + `mochi-modes-client` 已落地并进 26 项白名单，原「由用户自己接手，不派发」已过时） | 第 5 章原则保留 |
| 9 | Teacher Productivity Tools | 🟡 PPT/Word/Excel 插件在位，soffice 依赖未清；PLAN→CREATE→…→DELIVER 闭环未走通 | 第 24 章、P2 |
| 10 | 第一条 Work Golden Demo（班会 PPT 二次编辑不重做） | 🟡 class-meeting-prep 在位；"改第三页不重新生成"未验证 | P5 验收项 |
| 11 | Browser Chat Bridge | ⬜ 未做（dsh-browser 未装）；Selected Text Handoff 兜底思路保留；不绕登录/不盗 Cookie/不后台监听 | 答辩后 |
| 12 | Chat→Work Handoff | 🟡 **已实现**（2026-09-12 更正：对话/工作双模式用官方 `ctx.tools.restrict()` 收窄工具面，同一会话内切换） | — |
| 13 | Mochi Network UI | 🟡 对话内任务状态呈现已有；"任务协作网络"视图未做 | 第 8 章、后续阶段 |
| 14 | Approval UX | 🟡 审批闸在位；文案必须明确"准备做什么"待打磨 | P2 共用地基 |
| 15 | Teacher Skills 第一批 | 🟡 任务书 10 个：6 个在位（daily-brief/class-meeting/student-follow-up/weekly-report/material-find/movement）；teacher-message/schedule-coordination/classroom-support/document-preparation 映射进第 11 章清单 | 第三部分 |
| 16 | 嘉行联 Plugin | 🟡 mochi-campus 在位；未登录可正常办公✅；AUTH_REQUIRED 后续跑原任务未做 | 9.5 章、P2 |
| 17 | Memory UI | ⬜ 未做（查看/编辑/删除/清空 + "为什么 Mochi 知道这个"） | 第 38 章、P5 |
| 18 | Teacher Eval | ⬜ 未建（任务完成率/工具选择准确率/幻觉率/False Memory Rate 等 13 项指标） | P5 随 Golden Path 建最小集 |
| 19 | Golden Demo Set（6 条） | ⬜ 未做（传话/找卷子/换课/班会 PPT/"还是昨天那个格式"/外部 Chat→Work） | P5 验收清单（第 6 条依赖 11，可降级） |
| 20 | 学生端暂缓 | ✅ 与三端模型一致：本期不大做，Domain/Runtime/Memory/Dispatch 保持可复用 | 第 2/42 章 |

## 45. 工作事项总清单与终态路线

> 🔴 **时间已提前（2026-09-08 用户裁定）：外部截止 2026-09-30 → 2026-09-11（周五），本波次的人日估算不再适用。**
> **排期语境唯一事实源 = `docs/tasks/MOCHI-URGENT-REPLAN-2026-09-11.md`（L0-A）。**
> ⚠️ 以下「本期实施波次」章节自 2026-09-11 出包交付起**已不再适用**；
> 后续排期一律以 L0-A 为准，本段仅作历史追溯。
> 本轮（9/9–9/11）只保 P0 中"能演示"的最小集；**P3 教室端能力、P4 LAN 直连、P4.5 第二三项小样、P5 记忆 UI 全部移出本轮**（答辩时作"下一步"讲）。P0/P1/P2 三波压缩到三天，见紧急表 §3 倒排。

**本期实施波次（按优先级，前一条不通不做下一条）**

**波次时间盒（现行）**：P0 → 9/9 全天；P1（仅"双击即用"最小集）→ 9/10；P2 教师 Skills 三件事中**只保已完成的 PPT/成绩分析两项做演示**，组织信息导入延后；P3–P5 延后。

**P0 · 打地基（阻塞一切，1–2 天）**

| 事项 | 产出 | 验收 |
|---|---|---|
| **教室网段可达性实测**（模型 API / 校园服务，**第一优先**，不通则全盘调整） | 实测结论 | 不通则调整方案，不白做 |
| **教室机型与老师办公机系统摸底**（品牌/系统/内存/还原卡/麦克风；办公机 Win 还是 Mac） | 机型清单 | 附录 B1/B7 有答案 |
| git 基线提交 + 建 remote | 可回滚的基线 | `git log` 有基线，CI 可用 |
| 打包白名单 9→12 + 断言修复 | `prepare-mochi-resources.cjs` 同步 | 打包资源测试通过 |
| profile 资源空目录验证与修复 | 打包版能启动 | 打包版 mochi-web 起得来 |
| 冷启动计时基线 | 实测数字 | 写入 WORKLOG |

**P1 · 桌面端双击即用（3–5 天）**：启动页与进度 → 失败诊断页 → 首启向导 → 环境自检/Mochi 医生 → 四项零依赖硬改（含校园数据 API 云端化）→ **对话框文件拖入/加号上传** → 单实例+托盘 → 打包矩阵（arm64 先行，Windows 首包验证）→ U 盘全新机 UAT 10 场景全过。图标到位即换（不阻塞）。

**P2 · 教师 Skills 第一批三件事 + 组织信息导入（3–4 天）**：①纸质资料→可编辑电子文档（输入口=P1 的文件拖入）②成绩表→可复核分析 ③教案/材料→可继续改 PPT；④**组织信息批量导入**（服务器）+ 试点班跑通。共用地基先行（任务关联/审批/低置信对照）。每件事过"三句话验证"（第 14 章）。

**P3 · 教室端能力（3–4 天）**：打开软件+文件投屏 → **偏好引擎最小版（JSON 存储）**+课前预启动 → 大屏通知横幅 → 语音输入接入 → 大屏 UI（大字号/触摸）→ 学生角色工具面收敛。

**P4 · LAN 直连（3–4 天）**：peer 服务 → 信标发现 → 相识与治理 → dispatch transport=lan + 文件传输 → 五类场景测试（第 37 章）→ relay 降级联调。

**P4.5 · 教学建模最小样（可裁剪，2–3 天）**：按第 20 章方法做**一项**（建议圆锥曲线切线或平抛，以老师真实要讲的为准）→ JSXGraph 纯前端 → 教室端课堂模式展示 → 八步流程走一遍 → 教师确认"准确、能帮助讲解、课堂设备可用"。**时间不够则整体移到答辩后**（终态阶段 1 仍须完成）。

**P5 · 记忆与收口（2–3 天）**：world-state.md → SQLite 衰减层 → Memory UI 最小版 → Golden Path 总验收（任务书 6 条 Golden Demo，第 6 条"外部 Chat→Work"可降级为手动交接）+ Eval 最小集（任务完成率/工具选择准确率/幻觉率）→ Demo 脚本与视频。

**终态六阶段路线（《最终总纲》§二十）与本期波次的映射**

| 终态阶段 | 优先交付 | 本期落点 |
|---|---|---|
| 阶段 0：现状验收 | 真实 Harness/Skill/工具/身份/传话核查 | ✅ 已完成（PHASE 0/1）+ 本期 P0 |
| 阶段 1：教学建模小样 | 两位老师各一个真实教学问题的可操作模型 | P4.5（答辩前可裁剪，否则答辩后第一优先） |
| 阶段 2：教师完整工作链 | 资料电子化、成绩分析、PPT/Word、模型保存与续改 | P2 + P5 |
| 阶段 3：跨部门与成果协作 | 任务承接、审批、文件返回、场地设备协调、局部调课 | P4 + 答辩后 |
| 阶段 4：岗位与学生端扩展 | 教务/德育/后勤工作空间，学生模型探究与授权资源 | 答辩后（第 41/42 章终态） |
| 阶段 5：高级覆盖 | 更多学科模板、复杂自定义建模、完整排课、多人协作 | 答辩后（需明确需求、验证数据与资源预算） |

**已有阶段部分完成的，验收后直接复用，不要求机械重做。**

## 46. 里程碑（对齐 9-30 答辩）

> ⛔ **本节排期已作废（2026-09-12 补加，正文一字未删）**：外部已于 2026-09-08 把截止提前到 **2026-09-11**，本表周次不再适用。现行排期以 `docs/tasks/MOCHI-URGENT-REPLAN-2026-09-11.md`（L0-A）为准。

| 时间 | 节点 |
|---|---|
| 第 1 周（9/7–9/13） | P0 完成 + P1 主体完成（双击即用的 App 能在全新机跑通） |
| 第 2 周（9/14–9/20） | P2 三件事 + 组织导入 + P3 教室端能力 |
| 第 3 周（9/21–9/27） | P4 LAN 直连 + P4.5 教学建模小样（若做）+ P5 记忆与收口 |
| 答辩周（9/28–9/30） | 功能冻结，真机演练（教室一体机实测）、Demo 视频、答辩材料 |

## 47. 风险登记

| 风险 | 等级 | 对策 |
|---|---|---|
| 教室一体机机型/系统未知（可能 Win7、Android 大屏、还原卡） | 高 | P0 先摸机型；Win7→降级 Electron；Android→校内服务器+浏览器形态备选 |
| 教室网段不通外网（模型 API 不可达） | 高 | P0 第一优先实测；不通则评估校内代理/本地小模型兜底 |
| 打包链路从未验收（白名单/profile 空目录两个已知雷；Windows 的 junction/node-pty/SmartScreen 全新） | 高 | P0 第一件事 + P1 内 Windows 首包验证；SmartScreen 用"更多信息→仍要运行"说明 |
| 教学模型正确性（计算错、假设藏代码、看起来成立实则改了条件） | 高 | 八步流程强制（第 18 章）：参考值对照、假设显式、教学检查与正确性两项验收分开；超模板进"自定义草稿" |
| 图像理解/OCR 质量不达标（第一批第一件事依赖） | 中高 | 低置信对照模式托底；先做文档数字化再做试卷；真机样张先测；可复用 PP-StructureV3 但不当作无误重建 |
| 时间不够（距答辩约 3 周） | 中 | 严格按 P0→P5 顺序；可裁剪顺序：P4.5 教学建模小样 → P4 文件传输（留消息+通知）→ P5 Memory UI |
| 内置共享模型密钥被打满/外泄 | 中 | 设置页可换 key；监控 cached_tokens 与用量；泄露则轮换重发；管理密钥不打包 |
| 希沃/C30 登录态不持久（假设未验证） | 中 | 真机验证；不行则首次人工登录，Mochi 只负责打开 |
| 教室设备图形性能不足（三维/复杂仿真跑不动） | 中 | 二维 JSXGraph 优先；Three.js 三维实测后再上；降级静态配图 |
| 学生滥用教室端（刷问答/课堂秩序） | 低中 | 大屏公开可见+老师在场天然约束；必要时时段策略/禁用 |
| 图标专人交付延期 | 低 | 占位图标先行，不阻塞 |

## 48. 最终验收标准（一项能力"完成"需要什么证据）

每项能力都记录：**输入、实际调用、输出、验证方法、耗时、资源与模型费用、已知限制、复现步骤**。教学模型额外记录：**假设、参数范围、参考值、数值误差、教师审核版本和课堂可用性**。

至少通过以下检查：

- [ ] 老师换一种正常说法，仍能理解，不依赖固定短语。
- [ ] 输出是真文件或真模型，不是伪造的链接和成功话术。
- [ ] 修改参数后，图形、数值、曲线与解释保持一致。
- [ ] 无效参数、缺失资料、未认证和权限不足能够正确处理。
- [ ] 任务可以取消、恢复，重试不重复产生重要影响。
- [ ] 分享、审批和业务状态具有真实记录。
- [ ] 在目标 Mac、Windows 和课堂设备上实际验证。
- [ ] 减少动态效果或无法运行高阶图形时，仍有可用表达。
- [ ] 原有校园业务没有因新功能回归。
- [ ] 老师能够指出它实际减少了哪些步骤、帮助讲清了什么。

**"提高了多少教学效果""节约了多少时间"只能根据实际试用记录报告，不预先填写宣传数字。**

---

# 附录

## A. 文档来源对照表

| 来源文档 | 处置 | 并入章节 |
|---|---|---|
| `Mochi-方案总纲.md`（16 章） | 已吸收核心（定位/四原语/权限模型/dsh 地基/源码策略/战略对齐）；运行备忘与验证记录留原文件 | 第 1、6、7、8 章 |
| `Mochi-桌面端原生化方案.md`（23 章） | 已吸收核心；安装向导文案、UAT 10 场景详表、自检 11 项明细以原文件为准 | 第 21–26 章 |
| `第二阶段/记忆系统调研_2026-09-06.md` | 已吸收落地设计；学术机制与开源横评明细留原文件 | 第 38–40 章 |
| `Mochi 教师工作 Skills 清单与推进顺序 V1`（附件） | 已全量吸收并按现状对照 | 第 10–14 章 |
| Codex 任务书（PHASE 0–20，附件） | 已转为现状对照表；产品判断与工程原则并入第 1/14 章 | 第 44 章 |
| 《嘉行联 · Mochi 最终能力与功能总纲》（附件） | 已全量吸收：教学建模、岗位能力、学生端终态、成果共享、视觉体系、开源基础、安全底线、六阶段路线、验收标准 | 第 1、4、5、8、9、13、15–20、41–43、45、48 章 |
| `1.5阶段任务/README.md`（遗留收尾清单） | 已并入 P0/P1；原文件保留作执行打勾清单 | 第 45 章 |
| `foundation/` 37 份（Reality Audit / Codex Phase 0–12 / 验收） | **保持独立**，角色=实现工作票；与本文冲突时以本文为准 | — |
| `MIGRATION_AUDIT_REPORT.md` / `A2A_RELIABILITY_REPORT.md` / `免费搜索工具调研_2026-09-06.md` / `评审导向系统评估_2026-09-04.md` | 专题报告，保持独立引用 | — |
| `plan/01–12`（旧迁移计划） | 已被 foundation 取代，仅存档 | — |

## B. 待确认问题清单

1. **教室一体机**：品牌/型号/系统版本/内存、有无麦克风摄像头、能否装软件、有无还原卡？（决定 P1/P3 全部形态）
2. **教室网络**：教室网段能否访问模型 API 与校园服务？办公室↔教室是否同 VLAN？（决定 P0 实测清单与 LAN 发现方案）
3. **内置模型密钥**：出厂内置受限默认 key（MIMO 主力 + 智谱可选），老师可在设置页更换；管理密钥不打包——是否照此执行？
4. **记忆作用域** `scope_id`：按班级 / 按教师 / 全局单库？
5. **图标专人绘制**：进度与交付时间？（不阻塞，仅问状态）
6. **CI**：x64 与 Windows 走 GitHub Actions 还是借实体机？（需先建 remote）
7. **老师办公机系统分布**：Windows 还是 Mac？（决定打包主力顺序——现方案 arm64 Mac 先行仅因开发机是 Mac）
8. **课表数据来源**：mochi-campus 是否有教师课表 / 手动录入 / 正式系统导出？
9. **希沃/C30 实机情况**：教室一体机是否已装？exe 路径？登录态是否保持？
10. **组织信息导入的名录格式**：现有学生/老师名单是什么格式（Excel？学籍系统导出？），谁来做管理员导入？
11. **教学建模首批题目**：哪两位老师、分别出什么真实教学问题？（决定 P4.5 小样选题）

## C. 术语表

- **dsh / Mochi Harness**：Mochi 的内核运行时（源码集成，锁 tag）。
- **profile**：一套插件+配置的命名组合（headless / mochi / mochi-web）；同一套代码，不同身份。
- **对话即界面**：所有交互在 Mochi 对话框完成，不做独立面板。
- **审批闸**：敏感动作必须人工确认后才执行（人决定，对话内呈现）。
- **relay**：校园服务器中转的一句话传话通道（本方案后被降级为 fallback）。
- **LAN 直连**：第七部分的设备直连方案——A2A 的局域网传输通道（发现/相识/传输/融合四层）。
- **DSH_HOME**：Mochi 运行时数据根（`~/.mochi-home`）。
- **Artifact / 成果**：Mochi 产出的正式工作产物（文件、模型、计算结果等），统一进成果管理（第 43 章），有来源/版本/权限/验证记录。
- **教学模型**：第四部分的可交互教学成果；五种形态（静态示意/交互数学模型/物理仿真/数据建模/分步动画），视频与三维图片不算交互模型。
- **观察不是证明**：数学建模铁律——拖动很多次都成立只能支持猜想，证明必须走推导模式并说明使用条件。
- **Golden Demo**：任务书定的 6 条端到端验收场景（传话/找卷子/换课/班会 PPT/"还是昨天那个格式"/外部 Chat→Work），P5 总验收以其为准。
